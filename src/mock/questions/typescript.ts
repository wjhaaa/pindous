import { Question } from "@/types/question";

export const typescriptQuestions: Question[] = [
  {
    id: "ts-001",
    title: "TypeScript 中 interface 与 type 的主要区别是什么？",
    type: "qa",
    answer: "interface 和 type 都可以描述对象结构，但存在关键差异。interface 只能声明对象类型，不能直接声明联合类型或原始类型别名，而 type 可以声明任意类型。interface 支持声明合并（同名接口自动合并），type 则报错。extends 和 implements 语法：接口通过 extends 继承其他接口，类型别名可通过交叉类型（&）来组合，但不能直接 extends 或 implements。此外，interface 生成 .d.ts 时性能更好，因为接口是按名称索引的，类型别名只是别名。最佳实践：主要用于对象形状时优先使用 interface，需要联合类型、映射类型或复杂类型运算时使用 type。",
    explanation: "面试中常问区别，补充点：interface 能合并，type 不能；type 可以声明元组、联合类型等。误区：以为 type 不能被 extends，其实可以通过交叉类型模拟。现在 TypeScript 中两者功能越来越接近，但声明合并和性能差异仍是关键。",
    difficulty: 1,
    tags: ["类型基础","interface","type"],
    category: "TypeScript",
  },
  {
    id: "ts-002",
    title: "any、unknown、never 分别表示什么含义？如何使用？",
    type: "qa",
    answer: "any 表示任意类型，关闭类型检查，使用后变量可以赋值给任何类型或从任何类型赋值，常用于过渡或未知第三方库。unknown 表示未知类型，比 any 更安全，必须进行类型收窄（如 typeof、断言）后才能操作。never 表示永远不会出现的值，如函数永不会返回（抛出异常或死循环）或类型保护中不可能存在的分支。典型场景：any 快速开发但应避免滥用；unknown 用于安全地处理动态值（如 JSON.parse 返回值）；never 用于穷举检查，确保 switch 或条件分支覆盖所有情况。",
    explanation: "常见误区：把 unknown 当 any 随意操作，导致类型错误。最佳实践：优先使用 unknown 替代 any。never 可用来实现完整性检查，例如联合类型中排除所有已有类型后剩余 never。",
    difficulty: 1,
    tags: ["类型基础","any","unknown","never"],
    category: "TypeScript",
  },
  {
    id: "ts-003",
    title: "TypeScript 中的类型收窄（Type Narrowing）有哪些方式？请举例说明。",
    type: "qa",
    answer: "类型收窄是将宽泛类型缩小为更具体类型的过程。常用方式：1) typeof 操作符，如判断 typeof x === 'string' 将 x 收窄为 string；2) instanceof 用于类的实例；3) in 操作符检查属性是否存在；4) 字面量相等比较；5) 自定义类型守卫（is 谓词），函数返回 arg is Type，帮助 TypeScript 在条件分支后推断具体类型；6) 辨别联合类型，通过共同的 discriminant 属性（如 kind）区分联合成员。此外，asserts 守卫也能收窄。收窄使代码类型安全且智能提示更精确。",
    explanation: "类型收窄是 TypeScript 控制流分析的核心。面试官常要求写一个 is 类型守卫。误区：直接在未知类型上操作可能报错。最佳实践：对 unknown 类型使用 typeof 等收窄后使用。",
    difficulty: 2,
    tags: ["类型推断","类型收窄","自定义守卫"],
    category: "TypeScript",
  },
  {
    id: "ts-004",
    title: "联合类型（Union Types）和交叉类型（Intersection Types）有什么区别？",
    type: "qa",
    answer: "联合类型用 | 连接多个类型，表示值可以是其中任一类型，只能访问所有类型共有的属性（需收窄后方能访问特定成员）。交叉类型用 & 连接多个类型，表示值必须同时具备所有类型的成员，常用于组合多个对象类型。例如，A & B 是 A 和 B 的所有属性的合并，而 A | B 访问公共属性。常见场景：联合类型用于函数参数可接受多种类型；交叉类型用于混入（Mixin）或增强已有类型。注意交叉类型中属性冲突时会导致 never 属性。",
    explanation: "误区：混淆两者访问规则。联合类型需类型守卫后使用，交叉类型直接合并。补充：联合类型用于表示“或”，交叉类型表示“且”。面试中常结合泛型、条件类型考察。",
    difficulty: 2,
    tags: ["类型基础","联合类型","交叉类型"],
    category: "TypeScript",
  },
  {
    id: "ts-005",
    title: "泛型约束（extends 关键字）在 TypeScript 中如何使用？",
    type: "qa",
    answer: "泛型约束使用 extends 限制泛型参数必须符合某个类型。基本写法：T extends SomeType，表示 T 必须是 SomeType 的子类型。例如 function getLength<T extends { length: number }>(arg: T): number { return arg.length; }，约束 T 必须具有 length 属性。约束可以确保泛型内部能安全访问特定属性或方法。此外，可以使用 keyof 结合约束实现“在 K extends keyof T”模式，确保属性名安全。约束也支持联合类型约束和条件类型约束扩展。",
    explanation: "常见面试点：泛型默认值 `<T = string>` 可配合约束。使用约束时注意不要过度限制导致类型不灵活。extends 还可用于条件类型 `T extends U ? X : Y`，这是不同场景。",
    difficulty: 2,
    tags: ["泛型","泛型约束","extends"],
    category: "TypeScript",
  },
  {
    id: "ts-006",
    title: "如何理解 keyof 和 typeof 在 TypeScript 类型上下文中的用法？",
    type: "qa",
    answer: "keyof 是一个类型运算符，用于获取一个对象类型的所有键组成的联合类型（字符串字面量或数值字面量）。例如 type Keys = keyof { name: string; age: number } 得到 'name' | 'age'。typeof 在类型上下文中（不同于 JavaScript 的运行时 typeof）用于获取变量或属性的类型。例如 const obj = { a: 1, b: 'hello' }; type ObjType = typeof obj; 推断为 { a: number; b: string }。keyof 和 typeof 经常结合使用：type Keys = keyof typeof obj; 获取 obj 的键联合类型。这常用于映射类型和约束。",
    explanation: "keyof 只对类型使用，不能直接对值。typeof 可以捕获值类型。结合使用常见于动态键约束，如泛型 `K extends keyof T`。面试常要求手写 Pick 等工具类型。",
    difficulty: 2,
    tags: ["高级类型","keyof","typeof"],
    category: "TypeScript",
  },
  {
    id: "ts-007",
    title: "Partial、Required、Readonly 这几个工具类型分别有什么用？如何实现它们？",
    type: "qa",
    answer: "Partial<T> 将 T 所有属性变为可选，返回一个新类型。Required<T> 将 T 所有属性变为必选（去除可选修饰）。Readonly<T> 将 T 所有属性变为只读。实现原理：都使用映射类型。Partial: type Partial<T> = { [P in keyof T]?: T[P] }; 带 ? 修饰符。Required: type Required<T> = { [P in keyof T]-?: T[P] }; 用 -? 移除可选。Readonly: type Readonly<T> = { readonly [P in keyof T]: T[P] }; 添加 readonly。它们都是同态映射类型，保留原始类型的修饰符（如可选只读）需显式处理。常用于配置对象、状态管理等场景。",
    explanation: "面试常要求手写，注意 -?、-readonly 等修饰符。补充：Pick、Omit 也基于映射类型与 keyof。误区：Partial 只做浅层可选，深层需要递归实现 DeepPartial。",
    difficulty: 2,
    tags: ["工具类型","Partial","Required","Readonly"],
    category: "TypeScript",
  },
  {
    id: "ts-008",
    title: "TypeScript 条件类型中的 infer 关键字有什么作用？请举例说明。",
    type: "qa",
    answer: "infer 关键字用于在条件类型的 extends 子句中声明一个待推断的类型变量，常用于提取子类型。语法：`T extends Some<infer U> ? U : never`，如果 T 匹配 Some<X>，则推断出 X 类型并赋给 U。典型应用：实现 ReturnType<T>，通过 `T extends (...args: any[]) => infer R ? R : never` 提取函数返回值类型。同理可提取参数类型 Parameters<T>、Promise 包裹类型 Awaited<T> 等。infer 还可用于递归条件类型，如深度解包。是一个强大的类型编程工具。",
    explanation: "infer 只能在条件类型的真正分支中使用。面试常要求手写 ReturnType、Parameters。注意协变逆变位置推断行为不同，如函数参数位置逆变推断为交叉类型。",
    difficulty: 3,
    tags: ["高级类型","条件类型","infer"],
    category: "TypeScript",
  },
  {
    id: "ts-009",
    title: "如何实现一个通用的 DeepReadonly 工具类型？",
    type: "qa",
    answer: "DeepReadonly 递归地将对象所有层级属性设为只读。实现思路：遍历键，若属性值为原始类型则直接设为 readonly，若为对象类型则递归应用 DeepReadonly。定义：type DeepReadonly<T> = { readonly [K in keyof T]: T[K] extends object ? (T[K] extends Function ? T[K] : DeepReadonly<T[K]>) : T[K]; }; 这里要排除函数类型，因为函数也是 object，但通常不需要只读化。另一种严谨写法：type DeepReadonly<T> = T extends (infer R)[] ? readonly DeepReadonlyArr<R>[] : { readonly [P in keyof T]: DeepReadonly<T[P]> }; 需要处理数组场景。是条件类型和映射类型的综合运用。",
    explanation: "面试常考 DeepReadonly 或 DeepPartial 手写。注意边界：数组、函数、null/undefined（非 object）。恰当使用 extends 判断是否对象并递归。此外可借助 ts-toolbelt 等库但面试要求手写。",
    difficulty: 3,
    tags: ["工具类型","DeepReadonly","递归类型"],
    category: "TypeScript",
  },
  {
    id: "ts-010",
    title: "TypeScript 中 as const 和 satisfies 关键字分别用于什么场景？",
    type: "qa",
    answer: "as const 是类型断言，将对象、数组等标记为完全只读的字面量类型，所有属性值变为不可变的字面量类型，数组成为只读元组。常用于常量配置、Redux action type 等确保类型最窄化。satisfies 是类型操作符（4.9+），用于检测表达式是否符合某个类型，但不改变其推断类型，保留更精确的字面量推导。例如 `const config = { color: 'red' as const } satisfies Colors` 会检查 config 满足 Colors 类型，同时保留 color 为字面量 'red'，而直接使用类型注解 `const config: Colors = { color: 'red' }` 会导致 color 类型宽化为 string。satisfies 适合既要类型校验又要保留精确推导的场景。",
    explanation: "常见误区：混淆 as const 和类型注解。as const 完全锁定字面量，satisfies 仅检查兼容性但不扩大类型。面试可能问 satisfies 如何提升类型安全，举例路由配置等。",
    difficulty: 2,
    tags: ["类型基础","as const","satisfies"],
    category: "TypeScript",
  },
  {
    id: "ts-011",
    title: "TypeScript 中函数重载如何实现？与 JavaScript 的区别是什么？",
    type: "qa",
    answer: "TypeScript 通过多个函数类型声明（重载签名）和一个实现签名来定义函数重载。重载签名定义不同参数或返回类型的组合，实现签名需要使用最宽泛的类型来兼容所有重载。调用时 TS 按顺序检查重载签名，匹配第一个兼容的。JavaScript 没有原生重载，只能通过参数判断实现模拟，而 TS 在编译期提供类型保障，最终生成 JS 时只保留实现。",
    explanation: "实现签名不是重载的一部分，不能被直接调用。注意重载签名的顺序：更具体的签名应放在前面。常见误区是以为实现签名会参与类型检查，实际只有重载签名有效。重载常用于高精度函数，如根据参数类型不同返回不同结果，提升类型推断准确性。",
    difficulty: 2,
    tags: ["函数类型","函数重载"],
    category: "TypeScript",
  },
  {
    id: "ts-012",
    title: "TypeScript 中如何声明函数的 this 类型？有什么作用？",
    type: "qa",
    answer: "在 TypeScript 中，可以将 this 作为函数的第一个参数（伪参数）来声明类型，语法为 (this: 类型) 。这样可以在函数体内正确推断 this 的类型，防止错误使用。常用于回调函数、事件处理或明确指定调用上下文。编译后该参数会被移除，仅用于类型检查。",
    explanation: "使用时需注意：声明 this 参数后，函数调用时必须确保 this 指向正确类型，否则报错。这是 TS 针对 JS 动态 this 的增强，配合 --noImplicitThis 选项可强制要求。常见场景包括 Vue 选项式 API 中的 methods、React 类组件方法绑定等。",
    difficulty: 2,
    tags: ["函数类型","this类型"],
    category: "TypeScript",
  },
  {
    id: "ts-013",
    title: "在 TypeScript 中，implements 与 extends 有什么区别？",
    type: "qa",
    answer: "extends 用于类的继承，子类获得父类的方法和属性，并可重写（单继承），同时也可用于接口继承接口（多重继承）；implements 用于类实现接口，类必须提供接口中声明的所有成员，可同时实现多个接口。前者强调逻辑复用和层级关系，后者强调契约约束。",
    explanation: "extends 类时会继承实现，而 implements 只要求结构相同，不继承具体代码。接口不能使用 extends 类，但可以用 extends 接口。常见误区：以为 implements 会继承实现，实际只是类型检查。类可以实现多个接口，但只能继承一个类。",
    difficulty: 1,
    tags: ["类与接口","继承与实现"],
    category: "TypeScript",
  },
  {
    id: "ts-014",
    title: "TypeScript 中 abstract 类和方法的作用是什么？如何与接口区分？",
    type: "qa",
    answer: "abstract 类用于定义不能直接实例化的基类，可包含抽象方法（没有实现，要求子类重写）和具体方法。它提供部分默认实现，适合有共同逻辑的派生类。接口则只能定义形状契约，完全没有实现，类可通过 implements 实现多个接口。抽象类比接口更具体，可用于访问修饰符控制。",
    explanation: "抽象类可以包含 protected/public/private 成员，接口成员默认为 public。当需要同时提供默认实现和强制子类实现时用抽象类；仅需约束结构时用接口。注意抽象类可以继承普通的类，反之不行。",
    difficulty: 2,
    tags: ["类与接口","abstract"],
    category: "TypeScript",
  },
  {
    id: "ts-015",
    title: "TypeScript 的类型兼容性基于结构化类型，请解释并举例说明。",
    type: "qa",
    answer: "结构化类型（鸭子类型）指只要两个类型的成员结构兼容即可相互赋值，不关心命名。例如 interface A { x: number } 与 interface B { x: number; y: number } ，A 类型变量可被 B 类型对象赋值，因为 B 具备 x 属性。多余属性检查（如对象字面量直接赋值）是例外，会报错。",
    explanation: "结构化类型让 TypeScript 更灵活，与 C#/Java 的名义类型不同。赋值时目标类型只需是源类型的子集即可。但对象字面量会触发严格的多余属性检查，可通过中间变量或类型断言绕过。了解这个原理有助于避免类型错误。",
    difficulty: 1,
    tags: ["类型兼容性","结构化类型"],
    category: "TypeScript",
  },
  {
    id: "ts-016",
    title: "什么是 TypeScript 中的协变和逆变？在函数参数和返回值中如何体现？",
    type: "qa",
    answer: "协变指子类型可以赋值给父类型，如返回值协变：函数返回更具体的类型仍是父类型返回类型的子类型。逆变指父类型可以赋值给子类型（方向相反），如参数逆变：函数参数类型可以是更抽象的父类型。TS 中函数参数默认双向协变（宽松），开启 strictFunctionTypes 后参数变为逆变。",
    explanation: "示例：若 Animal > Cat，则 (): Cat 是 (): Animal 的子类型（返回值协变）；(animal: Animal) 是 (cat: Cat) 的子类型（参数逆变）。双向协变允许 (cat: Cat) 赋给 (animal: Animal)，但可能不安全，严格模式更推荐逆变。理解协变逆变有助于处理复杂类型关系。",
    difficulty: 3,
    tags: ["类型兼容性","协变逆变"],
    category: "TypeScript",
  },
  {
    id: "ts-017",
    title: "TypeScript 中 const enum 与普通 enum 的区别？编译产物有何不同？",
    type: "qa",
    answer: "普通 enum 编译为双向映射的对象，包含从名字到值和值到名字的映射，运行时存在。const enum 在编译时直接内联为字面量，不生成任何额外的 JavaScript 对象，不可反向映射，且只能在声明处访问。const enum 性能更高但不能动态遍历。枚举可用联合类型 + 常量对象替代。",
    explanation: "const enum 不能当作变量使用，例如不能 Object.keys() 取值。其成员必须为常量枚举表达式。当开启 isolatedModules 时，const enum 也会导致问题，建议用普通 enum 或联合类型替代。替代方案：用 as const 声明字面量联合类型。",
    difficulty: 2,
    tags: ["枚举","const enum"],
    category: "TypeScript",
  },
  {
    id: "ts-018",
    title: "declare 关键字和 .d.ts 声明文件有什么作用？",
    type: "qa",
    answer: "declare 用于告诉 TypeScript 编译器某些变量、函数、类或模块的类型，但不生成任何实际代码，通常在 .d.ts 文件中使用。.d.ts 声明文件仅包含类型信息，用于描述现有 JavaScript 库的 API，使 IDE 能提供智能提示和类型检查，也能扩展全局类型。",
    explanation: "例如 declare module '*.css' 声明非 JS 模块的类型。全局类型扩展可通过 declare global { interface Window { ... } } 实现。第三方库通常提供 @types 包，编写自己的 .d.ts 要确保 export 使用正确。注意 global 声明必须放在模块中（使用 export {}）。",
    difficulty: 1,
    tags: ["模块系统","declare"],
    category: "TypeScript",
  },
  {
    id: "ts-019",
    title: "tsconfig.json 中 strict 选项包含哪些子选项？各自作用是什么？",
    type: "qa",
    answer: "strict: true 开启一组严格检查，包含：strictNullChecks（禁止 null/undefined 赋值给非对应类型）、strictFunctionTypes（启用函数参数逆变检查）、strictBindCallApply（严格检查 bind/call/apply ）、strictPropertyInitialization（确保类属性在构造函数中初始化，需配合 strictNullChecks）、noImplicitAny（禁止隐式 any）、noImplicitThis（禁止 this 隐式 any）、alwaysStrict（使用 ES5 严格模式）。",
    explanation: "启用 strict 有助于早期发现潜在 null/undefined 错误，提升代码健壮性。常用配置：strict: true 再加 override 个别选项，如 skipLibCheck: true 跳过库文件检查，避免编译慢。建议新项目默认开启。",
    difficulty: 2,
    tags: ["TS 工程化","tsconfig"],
    category: "TypeScript",
  },
  {
    id: "ts-020",
    title: "在 TypeScript 项目中如何配置路径别名？发布 npm 包时如何处理类型声明？",
    type: "qa",
    answer: "路径别名通过 tsconfig.json 中 compilerOptions 的 paths 和 baseUrl 配置，例如 \"paths\": { \"@utils/*\": [\"src/utils/*\"] }。需配合构建工具（如 Webpack 的 resolve.alias ）使打包正确。发布 npm 包时，在 package.json 的 types 或 typings 字段指定入口 .d.ts 文件，通常输出到 dist 目录，或整体发布源码让用户通过 tsconfig 的 paths 或类型声明文件引入。",
    explanation: "确保编译打包时路径映射一致，常用 tsc --project . 配合 moduleResolution: node。发布前生成类型声明文件（ declaration: true ）。若源码不便发布，可发布编译后的 .js 和 .d.ts，并在 package.json 中正确声明 types 字段。注意避免路径别名泄漏到消费者环境。",
    difficulty: 3,
    tags: ["TS 工程化","路径别名","类型发布"],
    category: "TypeScript",
  },
  {
    id: "ts-021",
    title: "在 React + TypeScript 项目中，如何为函数组件定义 Props 类型？React.FC 和直接注解 Props 类型有什么区别？",
    type: "qa",
    answer: "定义 Props 类型有两种主流方式：1) 直接注解参数类型，如 function MyComp(props: { name: string; age?: number }) {...}，或结合接口先声明类型；2) 使用 React.FC<PropsType>，如 const MyComp: React.FC<Props> = (props) => {...}。主要区别在于 React.FC 会隐式包含 children 属性（ReactNode 类型），而直接注解需显式声明 children。React 18 后官方不再推荐 React.FC，因其可能引入不必要的 children 依赖，且对 defaultProps 的类型推断存在缺陷。推荐直接注解 Props 类型以获得更精确的类型控制。此外，可通过 ComponentProps<typeof Comp> 提取已有组件的 Props 类型复用。",
    explanation: "React.FC 源于类型自动补全 children 的便利，但在实际开发中很多组件并不需要 children，隐式包含会导致类型冗余甚至误用。直接注解方式更简洁可控，且与 TypeScript 原生行为一致。常见的误区是仍习惯使用 React.FC，尤其是在升级项目时遗留的代码。建议新项目统一采用直接 Props 类型标注，并在需要 children 时显式使用 ReactNode 类型。另外，注意 PropsWithChildren 工具类型可在直接注解时便捷添加 children。",
    difficulty: 2,
    tags: ["React + TS","Props泛型","React.FC"],
    category: "TypeScript",
  },
  {
    id: "ts-022",
    title: "如何在 React 中为常用的 Hooks（如 useState、useRef、useCallback）添加 TypeScript 类型？",
    type: "qa",
    answer: "useState: 大部分情况类型可推断，例如 useState(0) 自动推断为 number；初始值为 null 或联合类型时需手动指定泛型，如 useState<string | null>(null)。useRef: (1) 绑定 DOM 元素时使用 useRef<HTMLDivElement>(null)，current 为只读的 RefObject；(2) 存储可变值使用 useRef<number>(0)。useCallback 和 useMemo 通常可推断，复杂情况下可显式添加泛型参数，如 useCallback<() => void>(fn, deps)。useReducer 需为 reducer 函数和初始值提供类型。useContext 配合 createContext<T> 使用。useEffect 一般无需泛型。",
    explanation: "useRef 绑定 DOM 时，泛型参数为具体的 DOM 元素类型，且 useRef<HTMLDivElement>(null) 返回 RefObject，其 current 只读，不可再赋值；若需要可写，应使用 useRef<HTMLDivElement | null>(null) 得到 MutableRefObject。常见误区是对 useRef 初始值传入 null 但未联合 null 类型，导致后续赋值报错。useCallback/useMemo 多数场景无需显式泛型，因为它们能从函数和依赖列推导类型，过度显式标注反而降低可读性。useLayoutEffect 的用法与 useEffect 类型一致。",
    difficulty: 1,
    tags: ["React + TS","Hooks类型"],
    category: "TypeScript",
  },
  {
    id: "ts-023",
    title: "在 Vue3 + TypeScript 中，如何使用 <script setup> 配合 defineProps、withDefaults、defineExpose 进行类型声明？",
    type: "qa",
    answer: "在 <script setup lang=\"ts\"> 中，defineProps 可直接接收类型参数声明 Props：const props = defineProps<{ title: string; count?: number }>()。设置默认值需使用 withDefaults 包装，如 withDefaults(defineProps<Props>(), { title: 'default' })。defineEmits 通过泛型声明事件类型：const emit = defineEmits<{ (e: 'update', value: number): void }>()。defineExpose 显式暴露组件方法的类型，如 defineExpose<{ reset: () => void }>({ reset: () => {...} })。如需运行时验证，可结合 PropType 使用。Vue 3.3 后还支持更简洁的泛型默认值语法。",
    explanation: "Vue 的 Props 声明同时支持类型和运行时验证，但不冲突，类型定义主要用于编译器检查，运行时验证可通过对象语法（如 type: String）实现。常见误区是混淆 defineProps 的类型参数和运行时选项，或忘记 withDefaults 的用法导致默认值类型不匹配。defineExpose 指定类型参数可限制暴露的内容，父组件通过 InstanceType<typeof Component> 获取暴露对象的类型。注意 <script setup> 无需显式引入这些编译器宏，它们是编译时自动处理的。",
    difficulty: 2,
    tags: ["Vue + TS","defineProps类型","withDefaults","defineExpose"],
    category: "TypeScript",
  },
  {
    id: "ts-024",
    title: "在前端项目中，如何利用 OpenAPI/Swagger 文档自动生成 TypeScript 类型，并管理请求和响应的类型？常见工具有哪些？",
    type: "qa",
    answer: "常见工具有 openapi-typescript、openapi-generator、swagger-typescript-api 等。以 openapi-typescript 为例，将后端提供的 OpenAPI JSON 文件通过 CLI 生成 api-types.d.ts，其中包含 paths 对象精确描述了所有接口的请求参数与响应结构。使用时结合封装请求函数：import type { paths } from '@/api-types'; 然后定义类型安全的请求方法，如 request<paths['/users']['get']['responses'][200]['content']['application/json']>(url)。可避免手写接口类型，保证前后端类型一致。还可使用 openapi-fetch 等库直接获得类型安全的 HTTP 客户端。",
    explanation: "自动化类型生成可集成到 CI/CD，在构建前确保类型文件与最新合约同步。常见误区是手动维护类型导致不一致，以及忽略错误状态码的类型处理。响应类型通常为联合类型（如 200 和 401 的联合），可通过类型收窄处理不同状态。另外，推荐搭配 zod 做运行时校验，双重保障。生成的类型文件体积较大时，应只导入所需的结构，避免增大包体积。类型文件建议放在 src/types 目录并配置路径别名导入。",
    difficulty: 2,
    tags: ["API 类型管理","OpenAPI类型生成","请求响应类型"],
    category: "TypeScript",
  },
  {
    id: "ts-025",
    title: "TypeScript 5.0 引入了 Stage 3 装饰器，它与之前的 experimentalDecorators 有什么区别？如何实现一个简单的依赖注入装饰器？",
    type: "qa",
    answer: "区别：1) experimentalDecorators 基于早期提案，需要开启 --experimentalDecorators 编译选项，类装饰器接收目标构造函数，方法装饰器接收原型、属性名和描述符；Stage 3 装饰器是 TC39 新提案的装饰器，TS 5.0 默认支持，无需标志，使用 (value, context) 参数模式，context 包含 kind、name 等元信息，且不能修改类型（如不能在类装饰器返回新类）。2) Stage 3 装饰器更标准化，更安全。实现依赖注入：可用 reflect-metadata 配合装饰器存储元数据，例如 @Injectable() 标记类，@Inject(token) 注入属性。装饰器内部通过 Reflect.defineMetadata 记录依赖关系，DI 容器在实例化时根据元数据解析注入。",
    explanation: "Stage 3 装饰器目前仍需要 reflect-metadata 库来支持元数据，其设计初衷是减少装饰器对运行时的影响，注重元数据能力。常见的误区是将两个装饰器语法混用，导致编译错误。若要从旧装饰器迁移，需逐一调整签名，并注意类装饰器不能再返回新类。在 NestJS 等框架中仍广泛使用 experimentalDecorators，新项目推荐选择 Stage 3。实现 DI 时，注意装饰器不能在编译时注入，而是在运行时通过容器完成依赖解析。",
    difficulty: 3,
    tags: ["装饰器","TS 5.0 Stage 3 Decorators","experimentalDecorators区别","依赖注入"],
    category: "TypeScript",
  },
];
