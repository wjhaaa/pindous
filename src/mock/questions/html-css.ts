import { Question } from "@/types/question";

export const htmlCssQuestions: Question[] = [
  {
    id: "css-001",
    title: "请解释HTML5语义化标签的作用，并列举至少5个常用的语义化标签及其用途。",
    type: "qa",
    answer: "语义化标签是指具有明确含义的HTML元素（如<header>、<nav>、<main>、<article>、<section>、<aside>、<footer>），它们描述了内容的结构和意义。其作用包括：提升代码可读性，便于团队协作；增强可访问性（屏幕阅读器可快速定位页面区域）；利于SEO（搜索引擎更好地理解页面内容结构，提升排名）；使功能更清晰，例如<nav>用于导航链接。常用标签：<header>定义页面或部分的页眉；<nav>包含主导航链接；<main>标识页面核心内容（每个页面仅一个）；<article>表示独立、可复用的内容块（如博客文章）；<section>定义文档中的节，通常有标题；<aside>表示侧边栏或附属信息；<footer>定义页脚。",
    explanation: "误区一，将语义化仅视为“换标签”，实际上它改变了内容的意义。误区二，滥用<div>而不考虑更有意义的标签。在HTML5流行后，推荐使用语义化标签替代无意义的<div>包裹，尤其注意<main>不应嵌套在<article>或<aside>中。此外，语义化标签本身没有外观，样式需CSS控制，但它们在无障碍树中意义重大，也是SEO基础。",
    difficulty: 1,
    tags: ["HTML基础","语义化"],
    category: "HTML/CSS",
  },
  {
    id: "css-002",
    title: "请说明块级元素、行内元素和行内块元素的主要区别，并各举两个例子。",
    type: "qa",
    answer: "块级元素（block）：独占一行，宽度默认100%，可设置宽高、margin/padding。如<div>、<p>。行内元素（inline）：不独占一行，多个行内元素可排列在同一行，宽高由内容决定，无法设置width/height，上下margin/padding不影响布局（可设置左右margin/padding）。如<span>、<a>。行内块元素（inline-block）：综合两者特性，不独占一行，但可设宽高和margin/padding，兼具行内排列和块级盒子模型。如<input>、<img>（默认就是inline-block）。",
    explanation: "常见误区：<img>虽然是替换元素，但默认display是inline，实际行为却类似inline-block（可设宽高），因为它是原子行内级元素。可通过CSS的display属性转换类型。开发中常利用text-align:center来居中行内块元素，而不像块级元素需用margin:auto。理解盒子模型时，行内元素的margin和padding垂直方向不影响行高，但背景色会随padding延申。",
    difficulty: 1,
    tags: ["HTML基础","display"],
    category: "HTML/CSS",
  },
  {
    id: "css-003",
    title: "HTML5引入了哪些新的input类型？请至少列出5种，并简要说明它们的用途。",
    type: "qa",
    answer: "HTML5新增的input类型旨在提供更语义化的输入，优化用户体验和移动端输入。常见有：1. email：邮箱地址，移动端会调出邮件键盘，并可触发验证。2. url：网址，移动端调出URL键盘。3. number：数字，可定义min、max、step，显示数字调整按钮。4. date、time、datetime-local：日期、时间选择，调出原生日期/时间选择器。5. search：搜索框，可能显示清除按钮，触发搜索键盘。6. tel：电话号码，移动端调用电话键盘。7. color：颜色拾取器。8. range：滑块选择数值。这些类型在原生表单验证中也能简化开发。",
    explanation: "注意，这些类型在不支持的旧浏览器中会降级为text，因此需要渐进增强。虽然它们自带一些验证（如email格式），但服务端验证仍必不可少。对于number，移动端显示数字键盘，但输入非数字字符时浏览器行为各异。使用tel时不会验证电话号码格式，仅为键盘优化。在复杂场景中，可能仍需JavaScript增强。",
    difficulty: 1,
    tags: ["表单","HTML5"],
    category: "HTML/CSS",
  },
  {
    id: "css-004",
    title: "在HTML5中，如何实现表单的客户端验证？请列举至少三种验证相关的属性并说明其作用。",
    type: "qa",
    answer: "HTML5提供多种属性实现客户端验证，无需JavaScript即可检查输入。常用属性：1. required：标记必填项，提交时若为空则提示。2. pattern：使用正则表达式校验输入值，如pattern=\"[A-Za-z]{3}\"要求三个字母。3. type：不仅定义输入类型（如email、url），还自动提供格式验证。4. min/max：应用于number、range、date等，限制数值或日期范围。5. maxlength/minlength：限制输入字符数。6. step：与min/max配合，规定数字步长。浏览器会基于这些属性生成验证信息，并通过:valid/:invalid伪类改变样式。可配合setCustomValidity()方法自定义错误提示。",
    explanation: "客户端验证提升用户体验，但不能替代服务端验证。注意pattern中的正则表达式会自动加^和$，因此不需额外添加。使用novalidate属性可禁用表单验证（如用于草稿保存）。多个验证属性并存时，按顺序报错。常见误区：忽略type=\"number\"的输入可能返回字符串而非数字；pattern在空值时不会触发，需同时使用required。",
    difficulty: 2,
    tags: ["表单","验证"],
    category: "HTML/CSS",
  },
  {
    id: "css-005",
    title: "请解释script标签的async和defer属性的区别，并说明type=\"module\"对脚本加载和执行的影响。",
    type: "qa",
    answer: "async和defer都使脚本异步下载，不阻塞HTML解析。区别：async脚本在下载完后立即执行，中断解析，执行顺序不确定；defer脚本在HTML解析完成后按文档顺序执行。defer保证在DOMContentLoaded事件前执行。type=\"module\"的脚本默认行为类似于defer（延迟执行，按顺序），但其处于JavaScript模块作用域，自动采用严格模式。模块支持导入导出，且同模块只执行一次。它不阻塞HTML解析，执行时机同defer。可通过async属性让模块也完全异步执行（下载完立即执行，顺序不一定）。",
    explanation: "常见误区：认为async和defer都能保证不阻塞渲染，但async可能打断解析。动态插入脚本（document.createElement('script')）默认行为类似async，可设async=false顺序执行。在实际项目中，往往将非关键脚本用defer或async加载，关键脚本可用defer保持顺序。module脚本需要服务器支持正确的MIME类型（application/javascript或text/javascript）。",
    difficulty: 2,
    tags: ["script加载","async","defer"],
    category: "HTML/CSS",
  },
  {
    id: "css-006",
    title: "什么是ARIA属性？在前端开发中为什么重要？请举例说明role、aria-label、aria-hidden等属性的使用场景。",
    type: "qa",
    answer: "ARIA（Accessible Rich Internet Applications）属性用于增强Web内容可访问性，为辅助技术（如屏幕阅读器）提供额外的语义信息，弥补标准HTML标签含义不足的问题。重要性：确保残障用户能够理解和使用网页，尤其在动态内容、复杂组件和单页应用中。例子：role=\"button\"让一个<div>被识别为按钮；aria-label=\"关闭\"为没有文本的按钮提供描述；aria-hidden=\"true\"隐藏装饰性元素，避免干扰辅助技术。还可通过aria-expanded表示下拉菜单状态，aria-live区域用于实时更新通知。ARIA应作为原生语义的补充，不可滥用。",
    explanation: "黄金法则：优先使用语义化HTML，当原生元素不满足时才用ARIA。错误使用ARIA可能导致更差的可访问性，例如将role=\"button\"加在真实的<button>上。动态内容变化时，务必更新相应ARIA状态。Tabindex可用于控制键盘焦点顺序，但不要随意覆盖。颜色对比度也属于a11y，需通过工具检测。",
    difficulty: 3,
    tags: ["可访问性","ARIA"],
    category: "HTML/CSS",
  },
  {
    id: "css-007",
    title: "语义化HTML对SEO有哪些影响？另外，请简述Open Graph协议和Twitter Card的作用及基本用法。",
    type: "qa",
    answer: "语义化HTML通过使用正确的标签（如<header>、<article>、<h1>等）帮助搜索引擎理解页面结构和内容重点，从而提高关键词排名，也可能生成富媒体摘要。合理的标题层次（h1-h6）和语义标记让爬虫更高效抓取。Open Graph（OG）协议允许网页在社交平台（如Facebook）分享时显示标题、描述和图片，通过<meta property=\"og:title\" content=\"...\">等标签实现。Twitter Card类似，使用<meta name=\"twitter:card\" content=\"summary\">等，影响在Twitter分享的显示样式。两者都提升分享点击率，间接利于引流。",
    explanation: "在2015年后，语义化仍是SEO基础，但结构化数据（JSON-LD）更重要。OG和Twitter Card标签可共存，若缺失则社交平台可能自动提取。常见错误：忘记添加og:image的尺寸建议（1200x630），或twitter:card使用错误类型。确保meta标签在<head>中，且内容动态刷新时重新生成。",
    difficulty: 2,
    tags: ["SEO","语义化","Open Graph"],
    category: "HTML/CSS",
  },
  {
    id: "css-008",
    title: "什么是JSON-LD结构化数据？它如何帮助SEO？请给出一个使用JSON-LD标记文章页面的示例。",
    type: "qa",
    answer: "JSON-LD（JavaScript Object Notation for Linked Data）是一种嵌入HTML页面中的结构化数据格式，使用<script type=\"application/ld+json\">包含一个JSON对象，描述页面内容实体（如文章、产品、面包屑）。它帮助搜索引擎理解页面具体含义，从而在搜索结果中生成丰富摘要（如星级评分、发布日期），提升点击率和排名。示例：<script type=\"application/ld+json\">{ \"@context\": \"https://schema.org\", \"@type\": \"Article\", \"headline\": \"文章标题\", \"datePublished\": \"2025-01-01\", \"author\": { \"@type\": \"Person\", \"name\": \"作者名\" } }</script>。Google等主流搜索引擎推荐JSON-LD作为首选结构化数据格式。",
    explanation: "与Microdata和RDFa相比，JSON-LD无需在HTML元素中添加属性，维护方便，且解耦更好。务必使用Schema.org词汇。添加后用Google富媒体搜索结果测试工具验证。注意，JSON-LD中的双引号必须转义，但实际在script标签内只需按JSON规则。常见错误：只在部分页面添加，应覆盖所有重要页面。动态页面可使用JavaScript生成，但Google也能渲染SPA，但最好服务端注入。",
    difficulty: 2,
    tags: ["SEO","JSON-LD"],
    category: "HTML/CSS",
  },
  {
    id: "css-009",
    title: "请解释CSS中:has、:is和:where伪类的区别与使用场景，并给出代码示例。",
    type: "qa",
    answer: ":is()和:where()均用于减少重复选择器，简化写法。区别：:is()的特异性取参数中最高特异性选择器的值；:where()的特异性恒为0，因此更容易被覆盖，适合用作基础样式。:has()被称为“父选择器”，根据子元素或后续兄弟元素来选择父级或前置元素。示例：:is(header, main) p选择header或main下的p；:where(header, main) p作用相同，但优先级最低；article:has(img)选择包含图片的article。:has()还可组合兄弟选择器，如h1:has(+p)选择紧邻p的h1。目前主流浏览器均已支持。",
    explanation: ":is()和:where()解决了类似:-webkit-any的兼容性问题，:where()尤其适合用于重置样式或组件基础库，避免特异性过高导致难以覆盖。:has()突破了CSS长期只能向后选择的限制，但性能可能稍高，慎用复杂表达式。注意.:has()不能嵌套使用目前。实际中，:is()可用于统一定义主题样式。",
    difficulty: 3,
    tags: ["CSS选择器","伪类"],
    category: "HTML/CSS",
  },
  {
    id: "css-010",
    title: "CSS中:nth-child和:nth-of-type选择器有什么区别？分别举例说明。",
    type: "qa",
    answer: ":nth-child(An+B)基于父元素中所有子元素的索引计数，不论元素类型；:nth-of-type(An+B)仅计数同类型的兄弟元素。例如，<div><span>1</span><p>2</p><p>3</p></div>，div :nth-child(2)会选择第二个子元素<p>2</p>；div :nth-of-type(2)会选择第二个<p>即<p>3</p>，因为它只计数<p>类型。常用示例：li:nth-child(odd)选择所有奇数位置的li，而.container > :nth-of-type(2)选择容器内第二个该类型的元素。两者参数相同（支持odd/even、公式等），但计数范围不同。",
    explanation: "常见误区：:nth-child使用时，若前面有其他类型元素，可能偏离预期。在布局时光靠:nth-child选择奇数项可能出错，可改用:nth-of-type。实际开发中，结合class或标签名使用更精确。注意，:nth-child(even)和:nth-of-type(even)选择的对象可能不同，因为计数起点不同。理解CSS选择器匹配过程也有助于调试。",
    difficulty: 2,
    tags: ["CSS选择器","伪类"],
    category: "HTML/CSS",
  },
  {
    id: "css-011",
    title: "请解释CSS盒模型中的content-box和border-box的区别，并说明如何使用。",
    type: "qa",
    answer: "CSS盒模型决定了元素尺寸的计算方式。content-box是标准模型，width/height只设置内容区域，实际渲染宽度=width+padding+border+margin。border-box是IE模型，width/height包含content、padding和border，实际内容区域大小会减去padding和border，设置更直观，易于控制元素总尺寸。可通过box-sizing属性切换，默认content-box，通常推荐全局使用border-box以简化布局计算。",
    explanation: "box-sizing是继承属性，初始化时常用*{box-sizing:border-box;}。注意border-box下，padding不会撑大元素，内容区域自动缩小；content-box下设置内边距会撑大元素，可能导致溢出。常见误区是认为border-box也包含margin，实际上margin始终在width外。在弹性布局中，border-box更易预测。",
    difficulty: 1,
    tags: ["盒模型","box-sizing"],
    category: "HTML/CSS",
  },
  {
    id: "css-012",
    title: "什么是BFC（块级格式化上下文）？如何触发BFC？列举其常见应用场景。",
    type: "qa",
    answer: "BFC是独立的渲染区域，内部元素布局不会影响外部。触发方式：根元素、float不为none、position为absolute/fixed、display为inline-block/flow-root/flex/grid、overflow不为visible等。应用：1) 清除内部浮动（如父元素overflow:hidden）；2) 防止margin重叠（相邻垂直margin合并可通过将其中一个放入BFC解决）；3) 实现两栏自适应布局（利用BFC区域不与float重叠）。",
    explanation: "BFC垂直方向margin仍可能合并，但创建BFC后不会与外部元素合并。清除浮动更推荐使用clearfix伪元素，但BFC也有其用途。display:flow-root是专门创建BFC且无副作用的现代方式。IE下触发了hasLayout类似BFC。注意Flex/Grid容器本身创建了BFC，其子项内部布局遵循各自格式化上下文。",
    difficulty: 2,
    tags: ["盒模型","BFC"],
    category: "HTML/CSS",
  },
  {
    id: "css-013",
    title: "CSS Flex布局中，flex-grow、flex-shrink和flex-basis属性的作用分别是什么？",
    type: "qa",
    answer: "flex-grow定义项目的放大比例，默认为0，即使有剩余空间也不放大；设置为正整数则按比例分配多余空间。flex-shrink定义缩小比例，默认1，空间不足时项目将缩小，值越大缩小越多；0表示不缩小。flex-basis定义在分配多余空间前项目的初始尺寸，默认auto（参考width/height），可设为固定长度。简写flex属性推荐使用flex:1等于1 1 0%。三者共同决定弹性项目尺寸的灵活性。",
    explanation: "flex-basis与width/height的关系：如项目设置了width且flex-basis为auto，则以width为准；flex-basis设为具体值会覆盖width。flex:1等同于flex-grow:1;flex-shrink:1;flex-basis:0%，使得项目平分空间且忽略内容宽度。flex-shrink计算稍复杂，涉及基础尺寸与加权缩小。注意旧版IE和个别浏览器兼容性。",
    difficulty: 2,
    tags: ["布局","Flex"],
    category: "HTML/CSS",
  },
  {
    id: "css-014",
    title: "请解释CSS Grid布局中grid-template-areas、minmax()函数、auto-fill关键字和fr单位的用法与作用。",
    type: "qa",
    answer: "grid-template-areas通过命名区域直观定义网格布局，使用字符串表示行，区域名可跨行跨列。minmax(min, max)定义轨道尺寸范围，如minmax(200px, 1fr)表示最小200px，最大可用1fr弹性分配。auto-fill在重复函数repeat()中使用，根据容器大小自动填充尽可能多的列（或行），剩余空间留白；配合minmax实现响应式网格，列数自适应。fr单位表示弹性系数，按比例分配剩余空间，1fr 2fr表示一列占剩余空间1/3，另一列2/3。",
    explanation: "grid-template-areas需要与grid-area配合，每个区域必须矩形。auto-fill与auto-fit的区别：auto-fill会保留空轨道导致空白，auto-fit会折叠空轨道并拉伸已有列。使用repeat(auto-fill, minmax(250px, 1fr))可实现无媒体查询的响应式卡片布局。注意fr单位在绝对定位下不生效。Grid兼容性IE需前缀或使用旧语法。",
    difficulty: 3,
    tags: ["布局","Grid"],
    category: "HTML/CSS",
  },
  {
    id: "css-015",
    title: "CSS中position属性的五个取值static、relative、absolute、fixed和sticky各有什么特点？",
    type: "qa",
    answer: "static是默认值，元素在正常文档流中，忽略top/right/bottom/left和z-index。relative相对于自身原位置偏移，仍占据原空间，不脱离文档流。absolute脱离文档流，相对于最近的非static定位祖先元素定位，若无则相对于初始包含块（视口或html）。fixed脱离文档流，相对于浏览器视口固定，不随滚动移动。sticky是relative和fixed的结合，在阈值内relative，超出后fixed，需配合top等属性，且在父容器内滚动，不能超出父元素。",
    explanation: "sticky失效常见原因：未指定top等值；父元素overflow:hidden/auto/scroll会破坏sticky；父元素高度不够。absolute的包含块是最近的position非static的祖先，若无则是html或视口（取决于文档）。fixed定位在移动端可能因键盘弹出导致视口变化而异常。z-index仅对定位元素有效。",
    difficulty: 1,
    tags: ["定位与层叠","position"],
    category: "HTML/CSS",
  },
  {
    id: "css-016",
    title: "什么是CSS层叠上下文？如何创建层叠上下文？解释层叠顺序。",
    type: "qa",
    answer: "层叠上下文是HTML元素的三维概念，决定了元素在Z轴上的显示顺序。创建条件：根元素、z-index不为auto的定位元素、opacity小于1、transform/filter/perspective不为none、will-change指定这些属性等。每个层叠上下文内部按层叠顺序渲染：负z-index的定位元素 -> 块级元素/浮动元素 -> 行内元素 -> z-index:auto/0的定位元素 -> 正z-index定位元素。同一层级内部先绘制在底部，后绘制在上层。",
    explanation: "注意opacity<1也会触发层叠上下文，且其z-index视为auto，容易导致意外覆盖。层叠上下文的独立性使得内部元素无法比外部更高层级的元素更靠上，除非外部也提升层级。常见面试会问7种创建方式，包括flex/grid容器的子项设置z-index不为auto时（父容器创建），以及position:fixed等。层叠顺序新旧规范略有区别，但大体一致。",
    difficulty: 2,
    tags: ["定位与层叠","层叠上下文"],
    category: "HTML/CSS",
  },
  {
    id: "css-017",
    title: "请说明CSS中rem、em、vw、vh这几个相对单位的区别，以及calc()和clamp()函数的用途。",
    type: "qa",
    answer: "rem相对于根元素（html）的font-size，通常用于整体缩放；em相对于当前元素的font-size（字体大小）或自身font-size（其他属性），有继承叠加效应；vw相对于视口宽度的1%，vh相对于视口高度的1%。calc()允许动态计算长度，可混合不同单位，如width: calc(100% - 20px)。clamp()接受最小值、首选值、最大值，实现响应式区间，如font-size: clamp(1rem, 2vw, 2rem)，保证字体随视口变化但限制范围。",
    explanation: "em布局时容易嵌套导致大小累乘，现代推荐使用rem为主，但局部组件使用em更灵活。100vw包含滚动条宽度，所以全宽元素可能溢出，建议使用100%。移动端视口单位存在兼容问题（iOS Safari），使用vh时注意地址栏变化。calc()操纵符前后需空格。min()和max()类似，取最小/最大值。",
    difficulty: 2,
    tags: ["单位与函数","relative units"],
    category: "HTML/CSS",
  },
  {
    id: "css-018",
    title: "CSS中transition和animation有什么区别？如何使用transform实现一个沿X轴平移200px的动画？",
    type: "qa",
    answer: "transition用于样式属性变化时平滑过渡，需要触发事件（如:hover），只有开始和结束状态，不能循环。animation通过@keyframes定义多个关键帧，可自动播放、循环、反向等，更强大。实现平移动画：定义@keyframes slide { from { transform: translateX(0); } to { transform: translateX(200px); } }，然后应用 animation: slide 1s ease forwards;。也可用transition: transform 1s; 配合 hover时transform变化。",
    explanation: "transform不会触发重排/重绘（GPU加速），性能好。2D变换包括translate, rotate, scale, skew。配合will-change可提示浏览器优化。动画性能优化：使用transform和opacity；避免left/top等属性。贝塞尔曲线可自定义transition或animation的速率，如cubic-bezier()。FLIP技术处理动画性能。注意IE9不完全支持CSS动画。",
    difficulty: 2,
    tags: ["动画","transition","animation"],
    category: "HTML/CSS",
  },
  {
    id: "css-019",
    title: "如何使用CSS @media实现响应式布局？简述移动优先的设计原则。",
    type: "qa",
    answer: "@media根据设备特征（视口宽度、分辨率等）应用不同样式。常用语法@media screen and (max-width: 768px) { ... }。移动优先原则：先编写移动端基础样式（小屏），再用min-width媒体查询逐步增强为大屏样式。优点：减少移动端加载冗余代码，性能更好，并强制关注内容优先级。示例：基础样式适合移动，然后@media (min-width: 768px)添加平板布局，@media (min-width: 1024px)桌面增强。",
    explanation: "断点选取应基于内容而非特定设备，常用480px, 768px, 1024px, 1280px。可使用rem/vw单位配合flex/gird让布局更弹性。移动端1px边框效果可实现：伪元素+transform scale(0.5)。viewport meta标签设置width=device-width,initial-scale=1。现代响应式也可用clamp()或容器查询。避免使用!important，减少覆盖难度。",
    difficulty: 2,
    tags: ["响应式设计","@media","移动优先"],
    category: "HTML/CSS",
  },
  {
    id: "css-020",
    title: "什么是CSS容器查询（@container）？它与媒体查询有何不同？",
    type: "qa",
    answer: "@container允许根据直接父容器（或指定容器）的尺寸而非视口大小来设置样式。需要先声明容器上下文：container-type: inline-size (或size); 然后 @container (min-width: 400px) { 子元素样式 }。与媒体查询区别：媒体查询基于视口/设备特征，全局变化；容器查询基于组件所在容器，实现组件级别的响应式，更灵活，特别适合微前端、组件库中的自适用组件。浏览器支持日益完善（Chrome 105+, Safari 16+）。",
    explanation: "容器查询目前主要支持宽度查询（inline-size），高度查询（size）仍在推进。可配合container-name命名容器，区分不同上下文。容器查询弥补了媒体查询在组件复用时的不足，例如卡片在侧边栏和主内容区可能需要不同布局，无需依赖视口变化。结合flex/grid可实现真正的元件自适应。注意容器查询中的长度单位可用cqw (容器宽度百分比)等新单位。",
    difficulty: 3,
    tags: ["响应式设计","@container"],
    category: "HTML/CSS",
  },
  {
    id: "css-021",
    title: "如何使用CSS实现深色模式？请描述利用prefers-color-scheme和CSS变量的方案。",
    type: "qa",
    answer: "通过CSS媒体查询prefers-color-scheme检测系统主题，结合CSS自定义属性（变量）实现动态换肤。在:root中定义浅色变量，在@media (prefers-color-scheme: dark)中覆盖为深色变量，页面元素统一使用var()引用变量。例如：:root { --bg: #fff; --text: #000; } @media (prefers-color-scheme: dark) { :root { --bg: #222; --text: #eee; } }。也可结合JavaScript切换类名覆盖主题，并利用localStorage保存用户偏好，实现手动切换并记住选择。",
    explanation: "注意提供fallback值，如var(--bg, #fff)。仅依赖媒体查询无法手动切换，建议配合数据属性或类名策略，例如html[data-theme='dark']。系统自动模式可用matchMedia监听。常见误区：忘记处理非标准环境下变量的默认值，以及未考虑透明度和阴影的适配。",
    difficulty: 2,
    tags: ["深色模式","prefers-color-scheme","CSS变量"],
    category: "HTML/CSS",
  },
  {
    id: "css-022",
    title: "请解释CSS环境变量env()和viewport-fit=cover的作用，以及如何利用safe-area-inset-*处理iPhone刘海屏安全区域。",
    type: "qa",
    answer: "viewport-fit=cover让页面铺满整个屏幕，包括安全区域。env(safe-area-inset-top/right/bottom/left)是CSS环境变量，返回设备安全区域（如刘海、底部横条）的内边距值，用于给内容设置合适的间距，防止被遮挡。使用时，需在meta标签中设置viewport-fit=cover，然后通过padding: env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left); 或单独属性。例如：body { padding-bottom: env(safe-area-inset-bottom); }。",
    explanation: "iOS 11+和某些Android设备支持。旧版iOS使用constant()，但已废弃，应优先使用env()并同时提供constant()作为回退。注意：在无安全区域设备上env()返回0，设计时仍需保证可用性。常见误区：直接使用绝对定位而不考虑安全区域，导致按钮与系统手势冲突。",
    difficulty: 2,
    tags: ["安全区域","env()","safe-area-inset","viewport-fit"],
    category: "HTML/CSS",
  },
  {
    id: "css-023",
    title: "什么是回流（Reflow）和重绘（Repaint）？如何利用CSS的contain属性、content-visibility或transform等减少回流和重绘？",
    type: "qa",
    answer: "回流是元素几何属性改变导致浏览器重新计算布局，重绘是样式改变但不影响布局（如颜色变化）。回流代价高，会触发重绘。利用CSS的contain属性可以限制布局、样式、绘制的影响范围，如contain: layout paint;。content-visibility: auto延迟渲染视口外元素，大幅减少初始回流和绘制。使用transform和opacity操作，可通过GPU合成层直接处理，避免主线程回流重绘。另外，避免逐个操作样式，使用class批量修改也能减少回流。",
    explanation: "触发回流的常见操作：修改宽高、读offsetHeight等强制同步布局。尽量批量DOM更新、使用requestAnimationFrame。transform和opacity会提升到合成层，但过多层也会增加内存。will-change可预告优化，但不应滥用。contain属性和content-visibility是现代优化手段，需浏览器支持。",
    difficulty: 3,
    tags: ["回流与重绘","contain","content-visibility","合成层"],
    category: "HTML/CSS",
  },
  {
    id: "css-024",
    title: "display:none, visibility:hidden和opacity:0在隐藏元素时有哪些区别？请从文档流、事件响应、子元素继承、过渡动画等方面说明。",
    type: "qa",
    answer: "display:none：元素脱离文档流，不占据空间，子元素不可见，无法继承，不响应事件，不支持过渡动画。visibility:hidden：仍占据空间，子元素继承隐藏，但子元素可单独设置为visible而显示，不响应事件（子元素visible后可响应），可加过渡（visibility过渡需要配合opacity等）。opacity:0：占据空间，子元素继承透明度（但可单独设置不透明），仍可响应事件（透明区域可点击、可被键盘聚焦），支持过渡动画。实际项目中，常结合pointer-events:none与opacity:0实现视觉隐藏且不可交互。",
    explanation: "隐藏元素时注意无障碍：aria-hidden=true对屏幕阅读器隐藏，但不影响视觉。display:none也会从可访问性树移除。opacity:0元素仍可被键盘Tab聚焦，需加tabindex负值或pointer-events:none控制。过渡方面，visibility支持transition但需指定transition-property: visibility，变化无渐变过程。",
    difficulty: 1,
    tags: ["显示与隐藏","display","visibility","opacity"],
    category: "HTML/CSS",
  },
  {
    id: "css-025",
    title: "请解释Sass中@mixin与@extend的作用与区别，并说明各自的适用场景。",
    type: "qa",
    answer: "@mixin定义可复用样式块，通过@include调用，可接收参数生成动态样式，编译后会在每个调用处插入代码，导致重复但灵活。@extend用于继承选择器样式，将多个选择器用逗号合并，生成更少的CSS体积，但可能产生意料之外的大选择器和特异性问题。适用场景：需要参数化、媒体查询内、或生成大量变体时用mixin；仅需共享一组静态属性且选择器关系清晰时用extend，如按钮基础样式。",
    explanation: "使用@extend务必小心，它可能将选择器合并到文件末尾，影响级联顺序。应避免在@media内使用@extend，因为限制跨媒体查询继承。mixin可安全用于任何地方，gzip后重复代码代价较低，优先推荐mixin。在Sass 3.2后，可以混合使用，但需明确：extend适合“继承”语义，mixin适合“组件”复用。",
    difficulty: 1,
    tags: ["CSS工程化","Sass","mixin","extend"],
    category: "HTML/CSS",
  },
  {
    id: "css-026",
    title: "CSS Modules解决了什么问题？如何与React等框架集成？",
    type: "qa",
    answer: "CSS Modules通过构建工具将CSS类名编译为局部作用域的唯一哈希类名，解决传统CSS全局污染和样式冲突问题。在React中，需要配置webpack的css-loader开启modules选项，文件名通常为*.module.css。导入后得到一个对象：import styles from './style.module.css'，然后通过styles.myClass使用。支持组合（composes）特性实现样式复用，且可结合:global声明全局样式。Vue等框架也有类似支持。",
    explanation: "CSS Modules生成唯一类名，避免命名冲突，适合组件化开发。但它仍是纯CSS文件，不是CSS-in-JS，无动态样式能力。注意与TypeScript配合可使用*.module.css.d.ts生成类型声明。组合时，composes: classA from './other.module.css' 实现跨文件复用。常见误区：混淆CSS Modules与CSS-in-JS，或在全局样式中滥用。",
    difficulty: 2,
    tags: ["CSS工程化","CSS Modules"],
    category: "HTML/CSS",
  },
  {
    id: "css-027",
    title: "CSS @layer级联层的作用是什么？它与@scope的作用域有何区别？请举例说明使用场景。",
    type: "qa",
    answer: "@layer用于定义样式的层级顺序，通过控制级联优先级，解决大型项目中样式覆盖和特异性战争问题。层后面的样式优先级高于前面的，且未分层样式优先级高于所有层。@scope限定样式规则的作用范围，通过设定作用域根元素（<scope-start>）和（可选）排除元素（<scope-end>），使内部选择器仅在该子树中生效。区别：@layer管理优先级但不限制应用范围，@scope限制范围但不改变级联顺序。使用场景：@layer用于组织框架、基础、组件样式，如@layer reset, base, components；@scope用于组件内样式封装，避免泄露，如@scope (.card) to (.card-footer) { img { ... } }。",
    explanation: "@layer特性较新，需注意层顺序声明的先后，后声明的层优先级高，可配合import层次使用。@scope目前仍是草案，但Chrome等已支持，它可避免跨组件样式污染，并减少特异性依赖。常见误区：认为@layer能替代@scope实现作用域隔离，实际它只影响优先级，选择器仍可能匹配到其它元素。",
    difficulty: 3,
    tags: ["CSS工程化","@layer","@scope"],
    category: "HTML/CSS",
  },
  {
    id: "css-028",
    title: "请解释BEM命名规范，并说明其优缺点。如何与原子化CSS（如Tailwind）结合？",
    type: "qa",
    answer: "BEM即Block（块）、Element（元素）、Modifier（修饰符），命名模式为block__element--modifier，如menu__item--active。优点：语义明确，防止样式冲突，增强可维护性与团队协作；缺点：类名冗长，HTML体积增大，不擅长处理大量可复用样式的组合。与原子化CSS（如Tailwind）结合：可用BEM定义组件结构和状态，原子类负责具体装饰（间距、颜色等），减少冗余的BEM变体。例如，组件结构用bem，而布局细节用Tailwind类。或使用@apply将原子类组合到BEM类中，保留命名语义。",
    explanation: "BEM强调命名约定，不依赖任何工具；原子化CSS强调功能单一类，两者非对立。实践中，可在组件外层使用BEM命名类确保语义，内部细节必要时用原子类调整。注意避免在同一个元素上，BEM修饰符生成的样式与原子类产生覆盖冲突，通过设计规范统一。常见误区：认为原子化CSS就是滥用内联样式，实际它通过组合实现设计系统。",
    difficulty: 2,
    tags: ["CSS架构","BEM","原子化CSS"],
    category: "HTML/CSS",
  },
];
