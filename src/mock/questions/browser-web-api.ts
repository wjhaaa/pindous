import { Question } from "@/types/question";

export const browserWebApiQuestions: Question[] = [
  {
    id: "web-001",
    title: "如何优化大量的 DOM 操作？",
    type: "qa",
    answer: "优化大量 DOM 操作的核心是减少回流（Reflow）和重绘（Repaint）。常用策略：1. 使用 DocumentFragment 批量插入节点，一次性添加到 DOM 树，避免多次触发回流。2. 通过克隆节点或 display:none 进行离线操作，修改完成后再显示。3. 合并样式修改，使用 cssText 或 classList 批量更新类名。4. 避免频繁读取会导致强制同步布局的属性（如 offsetHeight、scrollTop），必要时缓存值。5. 对于大量列表渲染，使用虚拟列表或 requestAnimationFrame 分片处理。DocumentFragment 是一个轻量级的文档片段，可容纳多个子节点，但不在主 DOM 树中，因此对其的修改不会引发页面渲染，是最基础的优化手段。",
    explanation: "补充：操作 DOM 的代价很高，因为每一次修改都可能引起布局计算。现代框架（如 Vue/React）内部使用虚拟 DOM 和 diff 算法来最小化真实 DOM 操作。常见误区：误以为 innerHTML 比创建节点快，实际上 innerHTML 会重新解析字符串构建 DOM，且有 XSS 风险。最佳实践中，使用 createElement + appendChild 配合 DocumentFragment 更安全可控。",
    difficulty: 2,
    tags: ["DOM","DOM性能优化","DocumentFragment"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-002",
    title: "请说明 HTML5 History API 中 pushState、replaceState 和 popstate 事件的作用。",
    type: "qa",
    answer: "pushState(state, title, url) 向浏览器历史记录栈中添加一条新记录，并改变当前 URL，但页面不会重新加载。replaceState 类似，但用新记录替换当前历史记录，不增加栈深度。两者都允许携带一个状态对象 state，在后续 popstate 事件中被访问。popstate 事件在用户点击浏览器的前进/后退按钮或调用 history.go/back/forward 时触发，事件的 state 属性指向对应历史记录的状态对象。这些 API 常用于单页应用（SPA）的前端路由，通过监听 popstate 并配合 pushState 实现无刷新页面切换和 URL 同步。",
    explanation: "补充：调用 pushState 或 replaceState 本身不会触发 popstate 事件；只有浏览器导航动作才会触发。常见误区：误以为可以通过 popstate 监听到 pushState，实际需要手动派发或路由库自行处理。另外注意，两个方法的 url 参数受同源策略限制，且 state 对象大小不能过大（通常几百 KB）。一些 SPA 框架（React Router、Vue Router）封装了这些 API。",
    difficulty: 2,
    tags: ["BOM","history","pushState","popstate"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-003",
    title: "什么是事件委托？它利用了事件的什么特性？有哪些优点？",
    type: "qa",
    answer: "事件委托（Event Delegation）是将事件监听器绑定在父元素上，利用事件冒泡机制，当子元素触发事件时，事件会向上冒泡到父元素，父元素通过检查 event.target 来确定具体触发事件的子元素，从而执行相应处理。优点：1. 减少内存消耗，只需一个监听器管理大量子孙元素的事件。2. 动态绑定，无需为新增的子元素重新绑定事件。3. 简化代码，提升性能。事件委托依赖事件冒泡（如 click、mousedown），但并非所有事件都会冒泡（如 focus、blur 不会冒泡，可使用 focusin/focusout 替代）。常见应用场景包括列表、表格等。",
    explanation: "补充：使用事件委托时注意判断 target 是否符合预期元素，避免误触父级自身。通常使用 element.matches(selector) 进行过滤。误区：认为所有事件都能委托，实际上如 scroll、resize 等不会冒泡的事件无法使用委托。另外，React 等框架内部已对事件系统做了委托（合成事件），但开发者仍需理解其原理以优化手动操作。",
    difficulty: 2,
    tags: ["事件机制","事件委托","事件冒泡"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-004",
    title: "请解释 Cookie 的 HttpOnly、Secure、SameSite 属性的作用及安全意义。",
    type: "qa",
    answer: "HttpOnly：设置后，JavaScript 无法通过 document.cookie 读取该 Cookie，只能通过 HTTP 请求发送。可有效防止 XSS 攻击窃取 Cookie。Secure：标记为 Secure 的 Cookie 仅在 HTTPS 安全连接下传输，避免网络中间人攻击泄露 Cookie。SameSite：用于防御 CSRF 攻击。可选值 Strict（同站请求才携带，完全阻止第三方 Cookie）、Lax（大多数跨站请求不携带，但顶级导航 GET 请求会发送，较宽松）、None（允许跨站携带，必须同时设置 Secure）。合理组合这些属性可大幅提升 Web 应用安全性。",
    explanation: "补充：SameSite 默认值在 Chrome 等现代浏览器中已改为 Lax，若不显式设置可能导致第三方 Cookie 行为变化。常见误区：认为 Cookie 设置 HttpOnly 后前端就无法进行认证，实际认证仍然通过 HTTP 请求自动携带，只是防止 JS 读取。开发中，通常将 Session ID 型的认证 Cookie 同时设置 HttpOnly + Secure + SameSite=Strict/Lax。",
    difficulty: 2,
    tags: ["浏览器存储","Cookie","安全"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-005",
    title: "如何使用 Fetch API 发送请求并取消正在进行的请求？",
    type: "qa",
    answer: "Fetch API 本身返回一个 Promise，其内置的取消机制通过 AbortController 和 AbortSignal 实现。步骤：1. 创建 AbortController 实例 const controller = new AbortController(); 2. 在 fetch 的第二个参数中传入 signal: controller.signal。3. 需要取消时调用 controller.abort()。此时 fetch 的 Promise 会 reject 一个名为 AbortError 的 DOMException，可在 catch 中判断错误类型：if (error.name === 'AbortError') { /* 已取消 */ }。此外，还可通过 signal.aborted 属性判断是否已取消。注意，AbortController 同样可以取消其他异步操作，如流、事件监听等。",
    explanation: "补充：与老旧的 XMLHttpRequest 的 abort() 方法不同，Fetch 的取消机制更现代化且与 Promise 结合良好。常见误区：直接给 fetch 返回的 Promise 附加一个 .abort() 方法（这是很多第三方库的做法），原生 Fetch 并不支持，必须使用 AbortController。另外，要注意多个请求可以共用一个 controller，调用 controller.abort() 会取消所有使用该 signal 的请求。",
    difficulty: 2,
    tags: ["网络请求","Fetch API","AbortController"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-006",
    title: "DOM 中获取元素的常用方法有哪些？",
    type: "qa",
    answer: "常用方法：1. document.getElementById(id)：通过 ID 获取单一元素，速度快。2. document.getElementsByClassName(className)：返回实时更新的 HTMLCollection。3. document.getElementsByTagName(tagName)：通过标签名获取实时集合。4. document.querySelector(selector)：返回匹配 CSS 选择器的第一个元素，静态 NodeList。5. document.querySelectorAll(selector)：返回匹配的所有元素，静态 NodeList，支持 forEach 遍历但非实时。此外，还有 document.getElementsByName(name) 用于获取表单等 name 属性的元素。选择方法时，ID 获取最快，querySelector 更灵活但稍慢。",
    explanation: "补充：getElementsByClassName 和 getElementsByTagName 返回的是动态集合，即 DOM 变化时会自动更新，这可能导致遍历过程中的意外行为。而 querySelectorAll 返回静态快照，不受后续 DOM 修改影响。常见误区：混淆 HTMLCollection 和 NodeList，前者仅包含元素节点，后者可能包含文本节点等（部分方法返回）。实际开发中推荐使用 querySelector/querySelectorAll 因其支持复杂选择器，除非有性能极致需求。",
    difficulty: 1,
    tags: ["DOM","DOM操作"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-007",
    title: "localStorage、sessionStorage 和 Cookie 的区别是什么？",
    type: "qa",
    answer: "区别主要在存储容量、有效期、作用域和与服务器交互方面。存储容量：Cookie 通常限制在 4KB 左右，localStorage 和 sessionStorage 为 5MB 或更多。有效期：Cookie 可设置过期时间（Expires/Max-Age），未设置则为会话级；sessionStorage 仅存在于当前浏览器标签页或窗口，关闭即清除；localStorage 永久存储，除非手动删除。作用域：Cookie 和 localStorage 在同源的所有标签页共享，sessionStorage 仅在同一个标签页内共享。与服务器交互：Cookie 每次 HTTP 请求都会自动携带到服务器，而 Web Storage 不会，纯粹在客户端存储。",
    explanation: "补充：常用场景：Cookie 适合存储少量需要发送到服务器的数据（如身份令牌）；sessionStorage 适合存储单页会话数据（如表单临时输入）；localStorage 适合存储持久化配置、主题等。常见误区：误以为 localStorage 可以用来替代服务端 Session，因为它只在本地，无法跨设备同步。另外，Web Storage 只能存储字符串，复杂数据需 JSON 序列化。",
    difficulty: 1,
    tags: ["浏览器存储","localStorage","sessionStorage","Cookie"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-008",
    title: "addEventListener 的第三个参数可以是一个对象，其中的 passive、capture、once 选项分别有什么作用？使用 passive 有什么好处？",
    type: "qa",
    answer: "addEventListener 的第三个参数可以是布尔值（useCapture）或选项对象。passive: true 表示事件处理函数中永远不会调用 preventDefault()，浏览器可以提前知道，从而在触屏事件（如 touchstart）中立即开始滚动，避免等待 JS 执行完毕导致页面卡顿。capture: true 表示在捕获阶段触发监听器，而非默认的冒泡阶段。once: true 表示该监听器仅触发一次，执行后自动移除。被动事件监听器常用于滚动相关事件，能显著提升滚动性能。注意，若在 passive 监听器中调用 preventDefault() 将无效且控制台会警告。",
    explanation: "补充：移动端 Web 开发中，为 touchstart 和 touchmove 事件设置 passive: true 是优化滚动的关键。常见误区：addEventListener 的第三个参数如果传递 true 等同于 {capture: true}；但现代推荐使用对象形式以明确选项。此外，once 选项可以替代手动 removeEventListener，减少内存泄漏风险。",
    difficulty: 3,
    tags: ["事件机制","addEventListener","passive","capture","once"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-009",
    title: "XMLHttpRequest 和 Fetch API 的主要区别是什么？两者如何取消请求？",
    type: "qa",
    answer: "主要区别：1. Fetch 基于 Promise，支持 async/await，链式调用更简洁；XMLHttpRequest (XHR) 使用事件回调。2. Fetch 默认不携带 Cookie，需要设置 credentials: 'include'；XHR 自动发送同源 Cookie。3. Fetch 请求成功与否只看 HTTP 状态，即使 404/500 也会 resolve，需手动判断 ok；XHR 的 onerror 在发生网络错误时触发。4. Fetch 不支持原生进度监听，XHR 可通过 onprogress 实现。取消请求：XHR 使用 xhr.abort() 方法；Fetch 需要配合 AbortController，创建 controller 并将 signal 传入 fetch 参数，再调用 controller.abort() 取消。",
    explanation: "补充：Fetch 的流式处理（ReadableStream）比 XHR 强大，适合处理大文件下载。常见误区：Fetch 不会对非 2xx 状态抛出异常，常需封装判断。另外，XHR 的 abort() 之后会触发 abort 事件，而 Fetch 会抛出 AbortError。两者均受同源策略限制，但 Fetch 可配合 CORS 更灵活。",
    difficulty: 2,
    tags: ["网络请求","XMLHttpRequest","Fetch API","AbortController"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-010",
    title: "如何使用 CustomEvent 创建和触发自定义事件？适用场景有哪些？",
    type: "qa",
    answer: "使用 CustomEvent 分为两步：1. 创建事件：new CustomEvent(eventType, { detail: data, bubbles: true, cancelable: true }) 其中 detail 可携带任意数据，bubbles 控制是否冒泡。2. 触发事件：element.dispatchEvent(event)。监听方式与内置事件相同，通过 addEventListener(eventType, callback) 接收，回调中通过 event.detail 获取传递的数据。适用场景：组件间通信（非父子组件解耦）、自定义业务逻辑钩子（如列表刷新完毕触发自定义“refreshComplete”事件）、封装库向外暴露事件（如模态框打开/关闭）。它建立在 DOM 事件机制上，天然支持冒泡和捕获。",
    explanation: "补充：自定义事件是基于 DOM 事件系统的扩展，与普通事件一样可以阻止冒泡 (stopPropagation) 或阻止默认行为 (preventDefault)。常见误区：混淆 CustomEvent 与 Event 构造函数，实际上 Event 不能直接传递数据，需使用 CustomEvent。另外，Vue 等框架也有自己的事件系统，但原生 CustomEvent 可用于非框架环境或 web component。注意在分离的组件中，事件需在公共的 DOM 节点上触发和监听。",
    difficulty: 3,
    tags: ["事件机制","CustomEvent","自定义事件"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-011",
    title: "什么是浏览器的同源策略？它限制了哪些行为？",
    type: "qa",
    answer: "同源策略是浏览器最核心的安全机制，要求协议、域名、端口三者完全相同才算同源。它主要用于限制不同源之间的交互：1) 无法读取 Cookie、LocalStorage、IndexedDB 等存储数据；2) 无法通过 DOM 操作获取其他窗口的文档内容；3) 无法发送 AJAX 请求到其他域（默认禁止跨域）。其目的是防止恶意网站窃取用户信息。常见的跨域解决方案包括 CORS（服务端设置 Access-Control-Allow-Origin）、JSONP、WebSocket、postMessage 以及反向代理等。",
    explanation: "常见的误区是认为域名和子域名不同就算跨域（需端口或协议不同），实际上即使父子域名也不属同源。解决跨域时，CORS 是目前主流，JSONP 仅支持 GET 请求且存在安全风险。另外，通过设置 document.domain 可让主域名相同的框架间通信，但已逐渐被 postMessage 替代。",
    difficulty: 1,
    tags: ["浏览器安全","同源策略"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-012",
    title: "请说明浏览器强缓存与协商缓存的工作流程，并解释相关 HTTP 头部字段。",
    type: "qa",
    answer: "强缓存通过 Cache-Control（HTTP/1.1）或 Expires（HTTP/1.0）控制，若未过期则直接使用本地缓存，不发送请求到服务器，状态码 200（from disk/memory cache）。协商缓存则必须向服务器验证缓存是否可用，通过 If-None-Match （对应 ETag）或 If-Modified-Since（对应 Last-Modified）发起条件请求，若资源未修改返回 304 Not Modified，继续使用本地缓存；若已修改则返回 200 并重新下载资源。强缓存优先级高于协商缓存。",
    explanation: "Cache-Control 常用值：max-age（缓存最大有效秒数）、no-cache（走协商缓存）、no-store（不缓存）。ETag 是资源的唯一标识，比 Last-Modified 更精确（Last-Modified 只能精确到秒且某些情况无法检测变化）。实际开发中常结合两者，但需注意分布式系统下 ETag 的一致性。",
    difficulty: 2,
    tags: ["浏览器缓存","强缓存","协商缓存"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-013",
    title: "IntersectionObserver 的作用是什么？如何使用它实现懒加载？",
    type: "qa",
    answer: "IntersectionObserver 提供了一种异步监听目标元素与祖先元素或视口交叉状态变化的方法。当元素的可见比例（threshold）满足条件时触发回调，可在回调中执行相应逻辑，如懒加载图片：预先在 img 标签的 data-src 属性存放真实图片地址，当监听的目标占位元素进入视口时，将 data-src 赋值给 src 并发起请求，从而实现滚动到可视区域时才加载图片，减少首屏带宽和渲染压力。",
    explanation: "相比传统的 scroll 事件结合 getBoundingClientRect()，IntersectionObserver 不占用主线程，回调异步执行，性能更优。可配置 root（监听视口或某个祖先元素）、rootMargin（提前或延后触发）和 threshold（交叉比例）。注意 IE 不兼容，需要 polyfill 或降级方案。",
    difficulty: 1,
    tags: ["Observer API","IntersectionObserver","懒加载"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-014",
    title: "如何使用 Clipboard API 实现复制文本功能？",
    type: "qa",
    answer: "现代浏览器提供异步 Clipboard API，通过 navigator.clipboard.writeText(text) 将文本写入剪贴板，返回一个 Promise，成功时 Promise 解析，失败则 reject。需要页面处于安全上下文（HTTPS 或 localhost）且通常需要用户手势触发（如点击事件内调用），否则可能被拒绝。例如在按钮点击处理中执行 await navigator.clipboard.writeText('要复制的文本')。旧方案 document.execCommand('copy') 虽仍可使用，但已不推荐，因为它同步操作且可能阻塞页面。",
    explanation: "Clipboard API 还能读取剪贴板 readText()，同样需要权限。安全性要求严格，若页面不在 HTTPS 下，API 不可用。作为降级，可以在不支持 clipboard API 时使用 execCommand 方式，但需先创建一个 textarea 元素并选中其内容。此外，复制大量数据时注意异步操作可能被用户取消。",
    difficulty: 1,
    tags: ["Web API","Clipboard API"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-015",
    title: "MutationObserver 可以监听哪些类型的 DOM 变化？如何使用它？",
    type: "qa",
    answer: "MutationObserver 可以异步监听 DOM 树上的三种主要变化：1) 子节点的增删（childList）；2) 元素属性的修改（attributes）；3) 文本内容或注释的变化（characterData）。同时也能够监听子树（subtree）的变化。使用时先创建观察者实例并传入回调函数，然后调用 observer.observe(targetNode, config) 方法，config 指定需要监听的类型及选项。回调函数会在每次 DOM 变动后，接收一个 MutationRecord 数组，批量处理变动记录。完成后可调用 observer.disconnect() 停止监听。",
    explanation: "MutationObserver 取代了已废弃的 MutationEvent 事件，后者会同步触发且容易导致性能问题。观察者采用微任务队列异步执行，避免阻塞主线程。常见应用场景：懒加载动态插入的 DOM、监控水印篡改、配合框架实现组件更新等。注意不要过于频繁地操作大规模 DOM，以免回调执行过量导致卡顿。",
    difficulty: 2,
    tags: ["Observer API","MutationObserver"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-016",
    title: "Web Worker 是什么？如何利用它实现多线程编程？",
    type: "qa",
    answer: "Web Worker 是浏览器提供的后台线程机制，允许在独立线程中运行脚本，从而避免耗时计算阻塞主线程 UI 渲染。主线程通过 new Worker('worker.js') 创建 Worker，两者通过 postMessage 发送消息和 onmessage 监听对方消息实现通信，传递的数据会被结构化克隆拷贝。Worker 内部无法访问 DOM、window 对象、parent 等，但可以发起网络请求、使用 IndexedDB。专用 Worker 只能与创建它的页面通信，Shared Worker 可被多个页面共享。",
    explanation: "使用 Worker 时，要注意同源限制，且通信数据量大时序列化/反序列化可能带来开销，可借助 Transferable 对象（如 ArrayBuffer）转移所有权以提高性能。Web Worker 常用于密集计算、图片处理、大文件上传等场景。Service Worker 虽也是 Worker 的一种，但它主要用于离线缓存和推送通知，不能直接访问页面。",
    difficulty: 2,
    tags: ["Web API","Web Worker"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-017",
    title: "什么是浏览器的回流和重绘？哪些操作会引起回流？如何优化？",
    type: "qa",
    answer: "回流（Reflow）指渲染引擎重新计算元素的几何属性（位置、尺寸），重绘（Repaint）指元素样式改变但不影响布局时重新绘制像素（如 color、background-color）。回流一定会引发重绘，重绘不一定回流。引起回流的常见操作：修改宽高、边距、显隐（display），读取 offsetTop/scrollTop 等布局属性，改变窗口尺寸，访问某些计算样式的属性。优化方法：1) 使用 translate 代替 top/left 实现动画（跳过 Layout 和 Paint，只触发合成）；2) 合并多次样式修改（通过 class 批量修改或 cssText）；3) 离线操作（DocumentFragment、display:none 后操作再显示）；4) 避免频繁读取布局属性。",
    explanation: "现代浏览器已做优化，会把多次回流合并成一次，但强制读取布局属性会打断优化。创建图层（如 will-change: transform）可提升到合成层，避免回流。但滥用会占用过多内存，应只用于高频动画元素。使用 requestAnimationFrame 来调度动画也更有利于减少不必要的回流。",
    difficulty: 2,
    tags: ["浏览器渲染机制","回流","重绘"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-018",
    title: "什么是 XSS 攻击？它有哪些类型？分别如何防御？",
    type: "qa",
    answer: "XSS（跨站脚本攻击）是攻击者将恶意脚本注入到受信任的网站中，当用户访问该页面时脚本执行，窃取 Cookie、劫持会话等。主要三种类型：1) 反射型：攻击脚本通过 URL 参数等提交，服务端未过滤直接反射回页面；2) 存储型：恶意脚本存入数据库，用户请求页面时被输出；3) DOM 型：完全由客户端 JavaScript 处理数据导致（如 innerHTML 注入）。防御措施：对用户输入进行 HTML 实体编码或转义；设置 CSP 限制脚本来源；Cookie 设置 HttpOnly 防止脚本读取；使用安全的 DOM API（如 textContent 而非 innerHTML）；对 URL 进行校验。",
    explanation: "DOM 型 XSS 与反射型的区别在于前者不经过服务端，仅由前端脚本不安全的处理造成。CSP 可通过 Content-Security-Policy 头部精细控制脚本、样式等资源的来源，是深度防御的重要手段。开发框架（如 React）默认会转义大部分输出，但仍需注意 dangerouslySetInnerHTML 等危险操作。",
    difficulty: 2,
    tags: ["浏览器安全","XSS"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-019",
    title: "Service Worker 的缓存策略有哪些？如何实现一个离线缓存方案？",
    type: "qa",
    answer: "Service Worker 常见的缓存策略：Cache First（缓存优先，适用于不变静态资源）、Network First（网络优先，失败时回退缓存，适合 API 数据）、Stale-While-Revalidate（立即返回缓存同时后台更新，兼顾速度和新鲜度）、Network Only、Cache Only。实现离线缓存：在 install 事件中打开 Cache Storage 并预缓存关键资源（App Shell）；在 activate 事件中清理旧版缓存；在 fetch 事件中拦截请求，根据策略决定返回缓存还是发起网络请求并更新缓存。需要注册 Service Worker，监听生命周期事件并编写缓存逻辑。",
    explanation: "Service Worker 只能在 HTTPS 下使用（localhost 除外），因为它能够拦截网络请求，必须保证安全。更新机制：浏览器会比较 Service Worker 文件是否字节级变化，触发安装新版本，但激活需要通过 skipWaiting 和 clients.claim 控制。存储空间有限，应避免缓存过多资源导致 quota 超限。PWA 离线能力便是基于 Service Worker 实现。",
    difficulty: 3,
    tags: ["浏览器缓存","Service Worker缓存","离线缓存"],
    category: "浏览器与 Web API",
  },
  {
    id: "web-020",
    title: "什么是浏览器的合成层？如何触发 GPU 加速？有什么注意事项？",
    type: "qa",
    answer: "合成层是渲染引擎将页面中的某些元素提升为独立的图层，直接由 GPU 进行绘制和合成，跳过 Layout 和 Paint 阶段，仅触发 Composite，从而提高动画性能。触发条件包括：使用 3D transform（如 translateZ(0)、rotate3d）、will-change 属性指定 opacity/transform、video/canvas 等元素、CSS 动画或过渡中变化的 transform 和 opacity 属性。注意事项：过多或过大面积的合成层会占用大量 GPU 内存（层爆炸），可能导致比不提升更差的性能；will-change 应提前动态添加，动画结束后移除；低版本浏览器可能不支持，需评估兼容性。",
    explanation: "常见的优化是给需要做动画的元素添加 transform: translateZ(0) 或 will-change: transform，这被称为“开启了硬件加速”。但不是所有元素都该提升，隐式合成也会增加额外层。可以使用 Chrome DevTools 的 Rendering 面板查看层边界和内存占用。对于大量滚动列表，可结合 content-visibility: auto 和合成器优化。同时要注意 z-index 与层叠上下文对合成层的影响。",
    difficulty: 3,
    tags: ["浏览器渲染机制","合成层","GPU加速"],
    category: "浏览器与 Web API",
  },
];
