import { Question } from "@/types/question";

export const vueQuestions: Question[] = [
  {
    id: "vue-001",
    title: "请说明Vue2中响应式系统的原理及其缺陷",
    type: "qa",
    answer: "Vue2通过Object.defineProperty遍历对象属性，为每个属性定义getter/setter。getter中收集依赖（Watcher），setter中触发视图更新。缺陷包括：无法监听新增或删除属性，需用Vue.set/delete；无法监测数组索引变化和length修改，通过重写数组方法（push、pop等）弥补；初始化时深度递归遍历对象，性能开销大且无法跟踪动态添加的深层属性。",
    explanation: "数组监听是通过拦截七种原型方法实现的，因此直接通过索引修改或改变length不会触发更新。Vue.set内部也是调用defineProperty，对新增属性增加响应式。此外，Vue2中data声明必须预先列出所有属性，否则动态挂载的属性没有响应式。",
    difficulty: 2,
    tags: ["响应式系统","Object.defineProperty"],
    category: "Vue",
  },
  {
    id: "vue-002",
    title: "解释Vue3中Proxy如何实现响应式，并与Vue2对比有何优势",
    type: "qa",
    answer: "Vue3使用ES6 Proxy代理目标对象，在handler中拦截get（依赖收集）、set（触发更新）、deleteProperty等操作。无需遍历属性，惰性代理（仅在访问到对象时才对其做响应式处理）。优势：可检测属性增删、数组索引及length变化、支持Map/Set等；无需递归初始化，性能更优；可拦截多达13种操作，更全面。",
    explanation: "Vue3提供了reactive和ref两种API，reactive基于Proxy实现对象代理，ref用于基本类型，通过对象包装实现响应式。shallowReactive和shallowRef可避免深层响应式，用于优化大数据量场景。Proxy无法被polyfill，因此Vue3不再支持IE11。",
    difficulty: 2,
    tags: ["响应式系统","Proxy","ref/reactive"],
    category: "Vue",
  },
  {
    id: "vue-003",
    title: "为什么在Vue3中解构reactive对象会丢失响应式？如何使用toRefs解决？",
    type: "qa",
    answer: "解构reactive对象得到的是原始属性值的拷贝，拷贝后的变量只是普通值，失去了与代理的关联。toRefs将reactive对象的每个属性转换为一个ref对象，仍保持与原响应式对象的连接。示例：const { x, y } = toRefs(state); 此时.x和.value读写依然响应式。",
    explanation: "直接在模板或组合式函数中解构reactive会导致视图不更新。toRefs通常用于将reactive状态转为ref以便组合或解构。toRef针对单个属性转换，而toRefs转换整个对象。注意shallowReactive配合toRefs使用时，深层属性仍然可能是非响应式的。",
    difficulty: 1,
    tags: ["响应式问题","解构丢失响应式","toRefs"],
    category: "Vue",
  },
  {
    id: "vue-004",
    title: "为什么在Vue3中直接替换整个reactive对象的引用会导致响应式失效？如何解决？",
    type: "qa",
    answer: "reactive通过Proxy包装初始对象，返回的代理对象与原始对象建立响应式绑定。若直接对变量赋值为新对象（如 state = { a: 1 }），变量指向新对象，而原代理仍然指向旧对象，后续访问通过新引用无法触发响应式。解决方案：使用ref代替reactive，ref.value的整体替换能保持响应式；或者用Object.assign(state, newObj)合并属性避免替换引用。",
    explanation: "这是常见误区，很多开发者以为reactive和ref可以随意互换。ref内部对基本类型是包装对象，但对对象类型的值仍是调用了reactive，但是ref的.value可以被整体替换，因为它拦截了value的setter。因此对于需要整体替换的场景，推荐使用ref。",
    difficulty: 3,
    tags: ["响应式问题","整体替换"],
    category: "Vue",
  },
  {
    id: "vue-005",
    title: "讲述Composition API中defineProps和defineEmits的用法",
    type: "qa",
    answer: "两者是<script setup>中的编译宏，无需导入。defineProps用于声明组件的props，可传类型数组或对象进行校验，如 defineProps(['title']) 或 defineProps({ title: String, required: true })。defineEmits声明组件可触发的自定义事件，返回emit函数，如 const emit = defineEmits(['update:modelValue', 'submit'])，emit('submit', data)调用。在TS中可直接使用纯类型语法，编译器自动推导。",
    explanation: "从3.3+起，defineProps接受泛型参数简化纯类型声明，如 defineProps<{ msg: string }>()。注意defineProps和defineEmits必须定义在<script setup>顶层，不能放在函数内。此外，如果需要在props上定义默认值，可使用withDefaults辅助。",
    difficulty: 1,
    tags: ["组件基础","defineProps/defineEmits"],
    category: "Vue",
  },
  {
    id: "vue-006",
    title: "请解释Vue3.4引入的defineModel的作用，并对比v-model传统绑定方式",
    type: "qa",
    answer: "defineModel是编译宏，用于简化组件双向绑定。传统v-model需要父组件传递modelValue prop，子组件必须定义props和emits，并通过emit('update:modelValue')手动更新。defineModel自动创建一个ref，直接读写即可，编译器会生成对应的prop和事件。例如：const model = defineModel()；模板中直接使用v-model或model.value，极大简化代码。支持多个v-model及别名。",
    explanation: "defineModel从3.4版本起作为稳定功能，底层仍是modelValue和update:modelValue的语法糖。可以传递参数给defineModel指定不同的绑定名，如 defineModel('visible') 对应 v-model:visible。注意该特性需要开启props.destructure的实验性选项或使用Vue 3.4+默认支持。",
    difficulty: 3,
    tags: ["组件基础","defineModel"],
    category: "Vue",
  },
  {
    id: "vue-007",
    title: "v-for中为什么需要key？key的最佳实践是什么？",
    type: "qa",
    answer: "key作为每个节点的唯一标识，在虚拟DOM的diff过程中帮助Vue识别元素是否可复用，避免就地复用导致的渲染错误（如输入框状态错位）。没key时Vue采用“就地更新”策略，对于依赖状态的列表可能产生错误。最佳实践：使用每条数据都具备的唯一且不变的ID作为key；避免使用数组索引（index），尤其在列表顺序改变、插入或删除时索引会变化，导致不必要的重渲染和错误状态。",
    explanation: "不建议使用随机数作为key（如Math.random()），因为每次渲染都会生成新key，导致全部元素重新创建，丢失状态并大幅降低性能。对于静态列表且无顺序变化的场景，可以谨慎使用索引，但通常情况下唯一ID是最稳妥的选择。",
    difficulty: 2,
    tags: ["模板语法","v-for","key"],
    category: "Vue",
  },
  {
    id: "vue-008",
    title: "比较Vue中computed与watch/watchEffect的区别及使用场景",
    type: "qa",
    answer: "computed用于声明式计算衍生值，具有缓存机制，仅当依赖变化才重新计算，适合在模板中展示的派生数据，不能有副作用。watch显式指定监听源，接收新旧值，适合执行异步操作或精确控制副作用执行时机。watchEffect自动追踪依赖并立即执行回调，适合简洁地关联多个响应式数据的副作用，无需手动指定依赖。三者互补：computed聚焦派生值，watch/watcEffect聚焦副作用。",
    explanation: "常见误区：在computed中发起异步请求或修改其他状态（副作用）是错误用法，因为computed要求纯函数。watch默认惰性不立即执行，可通过immediate选项改变。watchEffect默认立即执行，可通过flush选项控制回调执行时机（如post使其在更新后）。在组合式API中，可结合ref和computed灵活管理派生状态。",
    difficulty: 2,
    tags: ["模板语法","computed","watch"],
    category: "Vue",
  },
  {
    id: "vue-009",
    title: "对比Vue2和Vue3生命周期钩子函数的命名变化，并说明setup中如何访问生命周期",
    type: "qa",
    answer: "Vue3中，beforeCreate和created被setup()本身取代。其余钩子名称加上on前缀变为onX形式：mounted -> onMounted，beforeDestroy -> onBeforeUnmount，destroyed -> onUnmounted等，语义更明确。在Composition API的setup中，需从vue导入这些钩子函数并传入回调；Options API中仍可使用旧名称（但建议新名称）。",
    explanation: "Vue3生命周期钩子可以在setup中多次调用，多个onMounted会按注册顺序依次执行。新增onRenderTracked和onRenderTriggered用于调试。在Composition API中，beforeCreate和created的逻辑可以直接写在setup顶层，因为setup本身就是在beforeCreate和created之间执行的。",
    difficulty: 1,
    tags: ["生命周期","Vue2 vs Vue3"],
    category: "Vue",
  },
  {
    id: "vue-010",
    title: "nextTick的实现原理是什么？在哪些场景需要用nextTick？",
    type: "qa",
    answer: "nextTick将回调函数延迟到下次DOM更新循环之后执行。Vue在数据变化后不会立即更新DOM，而是异步批量更新。nextTick内部优先使用微任务（Promise.then），不支持的浏览器降级为setTimeout，确保回调在DOM更新后调用。典型场景：数据更新后立即获取更新后的DOM节点（如获取元素尺寸）、在watch中操作依赖相同更新的DOM、执行依赖子组件挂载的逻辑等。",
    explanation: "Vue3中nextTick基于Promise，因此返回一个Promise，可搭配async/await使用：await nextTick()。常见错误是在修改数据后直接读取DOM（未等nextTick）得到旧值。在组合式API中，可以使用nextTick配合onMounted或onUpdated确保DOM就绪。",
    difficulty: 3,
    tags: ["生命周期","nextTick"],
    category: "Vue",
  },
  {
    id: "vue-011",
    title: "Vue3 中如何使用 provide 和 inject 实现祖先与后代组件通信？如何保证响应式？",
    type: "qa",
    answer: "在 Vue3 中，provide 和 inject 允许祖先组件通过 provide 提供数据，后代组件通过 inject 接收，实现跨层级通信。使用方式：祖先组件调用 provide('key', value)，后代组件通过 inject('key') 获取。为保证响应式，provide 的值应使用 ref 或 reactive 包裹，或者直接传递一个响应式对象，这样后代组件获取的仍然是响应式引用，数据变化会自动更新视图。还可以使用 readonly() 包装以防止子组件直接修改。与 Vue2 不同的是，Vue3 的 provide/inject 默认不响应式，必须显式传入响应式数据。",
    explanation: "常见误区：以为 provide 的值会被自动响应式处理，Vue3 中必须传入响应式对象。最佳实践：建议在 provide 时使用 provide('key', readonly(refValue)) 限制修改，保持单向数据流。注意 inject 可以设置默认值。在 Composition API 中，provide/inject 只能在 setup() 或 <script setup> 的同步阶段调用。",
    difficulty: 2,
    tags: ["组件通信","provide/inject"],
    category: "Vue",
  },
  {
    id: "vue-012",
    title: "解释 Vue Router 中导航守卫的生命周期和常用钩子，例如 beforeEach 和 afterEach，如何实现路由权限控制？",
    type: "qa",
    answer: "Vue Router 的导航守卫主要分为全局守卫、路由独享守卫和组件内守卫。全局前置守卫 beforeEach 在每次导航前触发，接收 to、from 和 next，可用于权限验证，例如检查用户是否有权访问目标路由，无权限则重定向到登录页。全局后置守卫 afterEach 在导航完成后触发，常用于页面标题更新、统计上报。路由独享守卫 beforeEnter 仅对单个路由生效。组件内守卫包括 beforeRouteEnter、beforeRouteUpdate、beforeRouteLeave，处理组件级逻辑。整个生命周期：导航触发 → 调用组件离开守卫 → 全局 beforeEach → 路由 beforeEnter → 组件 beforeRouteEnter → 解析异步组件 → 导航确认 → 全局 afterEach → DOM 更新 → beforeRouteEnter 的 next 回调。权限控制通常结合 token 或角色信息在 beforeEach 中判断，调用 next('/login') 或 next()。",
    explanation: "注意导航守卫的 next 函数使用：在 Vue Router 4 中，next 参数仍保留但被标记为可选，建议在守卫中返回 true 或 false 或目标路由字符串来实现放行或重定向。组件内守卫 beforeRouteEnter 无法直接访问 this，可通过 next 回调传入组件实例。权限控制要考虑路由懒加载，可将路由元数据 meta 用于权限标识。",
    difficulty: 3,
    tags: ["Vue Router","导航守卫"],
    category: "Vue",
  },
  {
    id: "vue-013",
    title: "Pinia 的 Setup Store 与 Option Store 有什么区别？分别适用于什么场景？",
    type: "qa",
    answer: "Pinia 支持两种方式定义 Store：Option Store 和 Setup Store。Option Store 类似 Vue 选项 API，通过 state、getters、actions 选项组织代码，结构清晰，适合中小型项目或习惯选项 API 的开发者。Setup Store 则使用类似 Composition API 的函数形式，通过 ref 定义 state，computed 定义 getters，普通函数定义 actions，更灵活，适合逻辑复杂、需要复用或 TypeScript 类型推断更好的场景。主要区别：Setup Store 可以更自由地组合逻辑，支持更高级的 TypeScript 类型推导，状态和逻辑更易拆分；Option Store 更结构化，上手简单。两者在性能和功能上等价，选择取决于团队偏好和代码组织需求。",
    explanation: "常见误区：认为 Setup Store 比 Option Store 更优越，实际上只是语法糖。在 Setup Store 中，this 不可用，需通过参数访问其他 store。Pinia 在内部会将两者转为同样的底层 store，因此可以混用。建议复杂业务逻辑选用 Setup Store，简单状态管理用 Option Store。",
    difficulty: 2,
    tags: ["Pinia","Store 模式"],
    category: "Vue",
  },
  {
    id: "vue-014",
    title: "keep-alive 组件的实现原理是什么？请说明 activated 和 deactivated 生命周期钩子的触发时机。",
    type: "qa",
    answer: "keep-alive 是 Vue 内置的抽象组件，用于缓存不活动的组件实例，避免重新渲染。原理：它在内部维护一个缓存对象和 keys 数组，根据组件名或 include/exclude 规则决定是否缓存。当组件被包裹在 keep-alive 中切换时，组件实例不会被销毁，而是调用 deactivated 钩子进入未激活状态；再次激活时调用 activated 钩子，并复用缓存的 DOM 和组件状态。缓存通过 LRU 算法管理，max 属性限制缓存数量，超出时销毁最久未使用的实例。activated 在组件被激活后调用（类似于重新挂载但不必重新创建），deactivated 在组件失活时调用（保留在内存中）。",
    explanation: "常见误区：认为 keep-alive 只缓存 DOM，实际上缓存整个组件实例，包括数据和计算属性。因此 activated 中可用于更新数据，deactivated 中做清理定时器等。注意 keep-alive 内部的组件必须有 name 选项，否则 exclude/include 无效。动态组件、路由视图等常配合 keep-alive 使用。",
    difficulty: 2,
    tags: ["内置组件","keep-alive"],
    category: "Vue",
  },
  {
    id: "vue-015",
    title: "Teleport 组件的主要应用场景有哪些？它与普通的组件渲染有何不同？",
    type: "qa",
    answer: "Teleport 是 Vue3 内置组件，允许将模板的 DOM 节点传送到目标元素下（如 body），打破组件树作用域限制。主要场景：模态框、弹窗、通知、下拉菜单等需要脱离父容器样式约束或 z-index 层级问题。使用 <Teleport to=\"body\"> 包裹内容，内容就会被渲染到 body 下。与普通渲染相比，Teleport 不改变组件逻辑和作用域（仍属于当前组件），但 DOM 物理位置改变，解决了 CSS 层叠上下文、overflow: hidden 裁剪等问题。多个 Teleport 可共享目标元素，内容按顺序追加。",
    explanation: "注意 Teleport 只是改变了 DOM 挂载位置，组件实例、props、事件监听仍属于原组件树。弹窗的关闭逻辑仍需在组件内部处理。移动端弹窗常需要固定定位，Teleport 可避免父容器样式干扰。to 属性也可接受 CSS 选择器字符串。禁用状态可通过 disabled 属性控制。",
    difficulty: 1,
    tags: ["内置组件","Teleport"],
    category: "Vue",
  },
  {
    id: "vue-016",
    title: "如何使用 defineAsyncComponent 实现组件异步加载？它与动态 import 结合使用时有哪些注意事项？",
    type: "qa",
    answer: "Vue3 提供 defineAsyncComponent 方法实现异步组件，接受返回 Promise 的工厂函数，用于按需加载组件。例如：const AsyncComp = defineAsyncComponent(() => import('./MyComp.vue'))。它支持配置加载中和错误状态组件：defineAsyncComponent({ loader, loadingComponent, errorComponent, delay, ... })。与直接动态 import 结合时，需要注意异步组件只有在需要渲染时才会加载，可结合路由懒加载或条件渲染实现代码分割。注意异步组件的加载状态处理，避免短暂空白。若组件加载失败，可重试机制通过 onError 回调实现。",
    explanation: "常见误区：直接将 import() 作为组件使用（如在 components 中声明为 () => import()），Vue3 中需用 defineAsyncComponent 包装。与 Suspense 配合能更好地管理异步依赖。注意类型推导：defineAsyncComponent 返回的组件类型与普通组件相同。另外，异步组件不能与 v-model 的默认 modelValue 冲突？无特别问题。",
    difficulty: 2,
    tags: ["性能优化","异步组件"],
    category: "Vue",
  },
  {
    id: "vue-017",
    title: "v-memo 指令的作用是什么？在什么场景下可以提升性能？请举例说明。",
    type: "qa",
    answer: "v-memo 是 Vue3.2+ 引入的指令，用于子树级别的记忆化缓存。它接受一个依赖数组，当依赖未变化时，Vue 将跳过该元素及其子树的虚拟 DOM 对比过程，直接复用上次渲染结果，从而优化性能。典型场景：大量静态内容或仅依赖特定数据变化的列表项。例如：<div v-for=\"item in list\" :key=\"item.id\" v-memo=\"[item.selected]\">，只有当 item.selected 变化时该项才重新渲染。注意 v-memo 需与 v-for 一起使用才能发挥最大效益，或者单独用于包裹仅依赖某个状态的静态区域。",
    explanation: "使用 v-memo 需要保证依赖数组准确，如果不包含所有动态绑定，可能导致视图不更新。它本质上跳过了 vnode 的 diff，适用于渲染开销大且更新频率低的场景。不建议滥用，因为记忆化本身也有开销。与 v-once 不同，v-once 只渲染一次永不更新；v-memo 可按条件更新。",
    difficulty: 3,
    tags: ["性能优化","v-memo"],
    category: "Vue",
  },
  {
    id: "vue-018",
    title: "Vue3 中如何使用 defineExpose 暴露组件内部属性或方法？在什么场景下需要谨慎使用？",
    type: "qa",
    answer: "在 <script setup> 中，组件默认是封闭的，父组件通过 ref 访问子组件实例时只能拿到 expose 暴露的内容。使用 defineExpose({ methodA, propertyB }) 显式声明公开的接口，父组件通过子组件的 ref 即可调用这些方法或访问属性。典型场景：父组件需要调用子组件的方法，如触发子组件内部的表单验证、滚动到指定位置等。但谨慎使用是因为这破坏了组件的封装性，增加了耦合，应优先通过 props/emits 通信。仅在必要时使用，并确保暴露的接口清晰稳定。",
    explanation: "注意：defineExpose 仅在 <script setup> 中可用。如果不用 <script setup>，则组件默认全部暴露。使用 expose 时，ref 绑定后需在 onMounted 后访问，因为 setup 执行时还未挂载。大量使用可能导致难以维护，建议作为最后手段。",
    difficulty: 2,
    tags: ["组件通信","defineExpose"],
    category: "Vue",
  },
  {
    id: "vue-019",
    title: "Vue Router 如何实现路由懒加载？结合动态 import，它如何帮助减小初始包体积？",
    type: "qa",
    answer: "Vue Router 通过动态 import() 实现路由级别的代码分割，即路由懒加载。在路由配置中，将 component 定义为 () => import('./views/User.vue')，构建工具会把该组件单独分割为一个 chunk，仅在路由被访问时才加载。这减少首屏需要加载的 JavaScript 体积，提升初始加载速度。Vite 或 Webpack 自动处理代码分割，支持命名 chunk（如 import(/* webpackChunkName: \"user\" */ './User.vue')）。结合路由守卫和骨架屏，可提供更流畅的用户体验。",
    explanation: "所有路由均使用懒加载是常见最佳实践，但注意首屏关键视图也可使用同步加载以避免加载延迟带来的闪烁。动态 import 返回 Promise，Vue Router 内部会处理好异步解析。懒加载的组件可以进一步使用 defineAsyncComponent 配置加载状态，但路由级别通常无需额外配置。",
    difficulty: 1,
    tags: ["Vue Router","路由懒加载"],
    category: "Vue",
  },
  {
    id: "vue-020",
    title: "请对比 Pinia 和 Vuex 在状态管理上的主要差异，为什么 Vue3 官方推荐 Pinia？",
    type: "qa",
    answer: "Pinia 是 Vuex 5 的提案实现，与 Vuex 4 相比：1) 去除了 mutations，直接通过 actions 修改状态，简化了流程；2) TypeScript 支持更好，类型推断友好，无需复杂包装；3) 模块化设计更平坦，无需嵌套 modules，每个 store 独立；4) 支持多个 store 实例，更好代码分割；5) 体积更小，且支持 Pinia 的持久化插件生态。Vue3 官方推荐 Pinia，因为它更符合 Composition API 思维，与 Vue3 的响应式系统深度结合，开发体验更佳。",
    explanation: "常见误区：Pinia 是全新的状态管理库，其实它底层仍使用 Vue 的响应式系统。与 Vuex 相比，actions 可直接调用，无 commit 概念。在 Pinia 中可以用 $patch 批量更新，也可以通过 $reset 重置状态。迁移时注意 Vuex 的 getters 根部参数变为 state 和 getters，用法略有差异。",
    difficulty: 1,
    tags: ["Pinia","Pinia vs Vuex"],
    category: "Vue",
  },
  {
    id: "vue-021",
    title: "请解释Vue3的快速Diff算法中，如何利用最长递增子序列（LIS）来最小化DOM移动？",
    type: "qa",
    answer: "Vue3的快速Diff算法在处理相同前缀和后缀节点后，对中间乱序部分使用最长递增子序列（LIS）来最小化移动。首先建立新节点在旧节点中的位置映射，计算出新节点顺序对应的旧索引数组，然后求该数组的LIS。LIS表示无需移动的节点序列，其他节点则按需移动到正确位置。通过倒序遍历新节点，若当前节点在LIS中则不移动，否则移动到上一个已处理节点之前。这样确保只移动必须移动的DOM，减少开销。",
    explanation: "LIS算法的思想是保留相对顺序不变的节点，只移动其他节点。Vue3的Diff采用双端比较和Key复用，结合LIS，相比Vue2的双端Diff更高效。常见误区是认为所有新节点都会重新排序，实际上LIS保留了静态数组。注意LIS算法的复杂度为O(n log n)，适用于大规模列表。",
    difficulty: 3,
    tags: ["Diff与编译优化","快速Diff","LIS"],
    category: "Vue",
  },
  {
    id: "vue-022",
    title: "在Vue3模板编译中，什么是静态提升和预字符串化？它们如何提升渲染性能？",
    type: "qa",
    answer: "静态提升是指将模板中不依赖响应式数据的静态VNode提升到渲染函数外部，避免每次渲染时重复创建，减少内存开销。预字符串化是将连续的静态内容（如纯文本和静态标签）编译为字符串，通过innerHTML直接设置，跳过VNode创建和Diff过程。两者结合可大幅减少虚拟DOM的创建和比对成本，尤其在大量静态内容时，初始渲染和更新性能显著提升。",
    explanation: "Vue3编译器会分析模板，标记静态节点和静态根节点。静态提升后，静态VNode只在组件初次渲染时创建一次；预字符串化会将静态内容直接编译为字符串字面量，在挂载时直接设置innerHTML，完全避免VNode开销。注意：带插值或动态绑定的节点不会被静态提升。",
    difficulty: 2,
    tags: ["Diff与编译优化","静态提升","预字符串化"],
    category: "Vue",
  },
  {
    id: "vue-023",
    title: "Vue3中h函数的作用是什么？如何使用h函数创建VNode？",
    type: "qa",
    answer: "h函数是Vue中用于创建VNode的渲染函数。它接收三个参数：标签名或组件、props对象、子节点（字符串、数组或插槽）。例如 h('div', { class: 'foo' }, 'Hello') 创建div元素。当模板无法满足动态逻辑时，可使用h函数编写渲染函数，返回VNode树。它与JSX类似，但更贴近Vue内部实现。使用h函数需注意手动处理响应式依赖，并避免在插槽中使用嵌套渲染函数。",
    explanation: "h函数是createVNode的别名，通常在render选项或setup中返回渲染函数时使用。与模板编译相比，手写h函数不够直观，但更灵活。常见误区是混淆h函数和createElement（仅接收标签），Vue3统一为h。在TypeScript中，h函数有类型推断。",
    difficulty: 1,
    tags: ["渲染机制","h函数","VNode"],
    category: "Vue",
  },
  {
    id: "vue-024",
    title: "在Vue3中如何封装一个通用的useFetch组合式函数？需要考虑哪些方面？",
    type: "qa",
    answer: "封装useFetch需处理请求状态（loading、error、data）、自动响应式、请求取消、并发控制等。基本结构：export function useFetch(url) { const data = ref(null); const error = ref(null); const loading = ref(true); fetch(url).then(...).catch(...).finally(() => loading = false); return { data, error, loading }; } 需注意：使用AbortController取消重复请求；利用watchEffect自动跟踪依赖触发请求；避免多次挂载重复请求；在SSR或路由切换时清理副作用。",
    explanation: "useFetch是组合式API的典型应用，与React Hooks的useEffect类似，但Vue自动收集依赖。常见误区：未处理组件卸载时的异步回调，导致内存泄漏；未将请求结果ref包裹，丢失响应式。最佳实践：使用axios+拦截器，结合Pinia管理全局请求状态。",
    difficulty: 2,
    tags: ["Composables","useFetch","设计模式"],
    category: "Vue",
  },
  {
    id: "vue-025",
    title: "Vue3的异步更新机制是如何实现的？请说明scheduler、任务队列和执行时机（包括nextTick的原理）。",
    type: "qa",
    answer: "Vue3内部使用调度器（Scheduler）管理异步更新。当响应式数据变化时，effect会被标记为脏并推入微任务队列（Promise.then/MutationObserver）。同一个effect在同一个同步操作中多次触发只会入队一次，实现批量更新。刷新队列时，按顺序执行effect，并可能触发子组件更新。nextTick将回调推入微任务队列，确保在DOM更新后执行。与Vue2类似，但Vue3利用Proxy更精准追踪依赖，减少不必要更新。",
    explanation: "Scheduler支持调整刷新时机：pre（组件更新前）、post（DOM更新后）、sync（同步）。默认组件更新为pre。nextTick的本质是将回调放入微任务队列，等待当前同步任务及队列清空后执行。常见误区：依赖nextTick获取最新DOM，但未考虑子组件也可能异步更新。",
    difficulty: 3,
    tags: ["调度机制","scheduler","job队列","nextTick"],
    category: "Vue",
  },
  {
    id: "vue-026",
    title: "请介绍Vue3自定义指令的生命周期钩子，并实现一个v-click-outside指令。",
    type: "qa",
    answer: "Vue3自定义指令钩子有：created（绑定元素属性初始化）、beforeMount（首次挂载前）、mounted（插入父节点后）、beforeUpdate（组件更新前）、updated（组件更新后）、beforeUnmount（卸载前）、unmounted（卸载后）。v-click-outside用于监听点击元素外部时触发回调。实现：在el上绑定document的事件监听，mounted时添加，unmounted时移除。通过event.target判断是否在el外部。注意需将回调保存在el._clickOutside上以便清理，并支持binding.value函数调用。",
    explanation: "v-click-outside常被用于下拉菜单、模态框点击外部关闭。常见误区：直接在el上使用addEventListener，但未在unmounted中移除，导致内存泄漏；忘记处理动态参数变化。最佳实践：使用事件委托或在文档上监听，并通过el.contains判断。",
    difficulty: 2,
    tags: ["自定义指令","v-click-outside","生命周期"],
    category: "Vue",
  },
  {
    id: "vue-027",
    title: "在实际项目中，如何通过Vue3自定义指令实现按钮级别的权限控制（v-permission）？",
    type: "qa",
    answer: "v-permission指令根据用户权限控制按钮可见性。实现思路：从全局状态（如Pinia）获取用户权限列表，在mounted钩子中检查当前按钮需要的权限标识是否在用户权限中，若无权限则移除或隐藏该元素。例如：binding.value为所需权限字符串或数组，在mounted中判断，若无权限则el.parentNode?.removeChild(el)。也可使用v-if但指令更解耦。需要监听权限变化时，可在updated中重新判断。",
    explanation: "这种指令常用于中后台系统，结合路由守卫和后端验证构成权限体系。常见误区：仅移除DOM而不销毁组件实例（导致内存泄漏），应确保组件已卸载。最佳实践：结合全局权限store，并在权限变化时使用update钩子重新校验，或通过提供无效化机制让组件重新挂载。",
    difficulty: 2,
    tags: ["自定义指令","v-permission","权限控制"],
    category: "Vue",
  },
  {
    id: "vue-028",
    title: "在使用Vue Test Utils进行组件测试时，mount和shallowMount有什么区别？分别在什么场景下使用？",
    type: "qa",
    answer: "mount会完整渲染组件及其所有子组件，而shallowMount仅渲染当前组件，将子组件存根为空白占位（stub）。shallowMount适合对组件进行单元隔离测试，避免子组件内部实现的影响；mount适合集成测试，验证组件间交互。例如测试父组件的逻辑时，使用shallowMount可以忽略子组件的复杂度，只关注父组件自身行为。但若需测试组件间数据流或插槽传递，则必须使用mount。",
    explanation: "shallowMount默认将子组件替换为存根，但可以通过stubs选项自定义存根行为。常见误区：在shallowMount中查找子组件的模板内容会失败，需使用mount或指定stub。最佳实践：单元测试尽量使用shallowMount，集成测试使用mount，并按需配置stubs。",
    difficulty: 1,
    tags: ["Vue测试","Vue Test Utils","shallowMount","mount"],
    category: "Vue",
  },
];
